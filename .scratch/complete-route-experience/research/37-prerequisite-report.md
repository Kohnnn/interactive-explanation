# W0 bounded authored-access prerequisites

## Delivered scope

Worktree `/tmp/opencode/complete-route-prerequisites9782bff`, starting at `9782bff`. Only two application files change: shared `prototype-visual.css` scoped to six synth lesson footers below 414px, and decision-tree's authored HTML. No package01, baseline, shared smoke tool, vendor engine, renderer backing dimension, canvas/SVG style, archive asset, or manifest change. No merge/push.

- All six synth routes: the direct flex-child footer had a 320px minimum-content language run plus 64px padding. Neutralized language links are adjacent inline spans. A bounded max-width/min-width override plus inline-block whole-label wrapping makes the footer and document 320px at 320px, without hiding or truncating text. Existing runtime overflow rules were neither added nor used as acceptance evidence. The 390px footer also reflows; desktop is unchanged.
- Decision-tree: its only home image/link was inside the shared visually collapsed legacy title, centered around x=-1. At 320px its image was x=-58.8125 to -8.8125. Move the existing 50x46 image/link into the authored hero instead of expanding the scrolly title, duplicating the link, or resizing a chart. At 320px the image is x=135 to185; keyboard Enter reaches the Atlas. Section IDs/data-index, compiled observers, scripts and intrinsic dimensions remain unchanged.
- Markov: no application fix needed for the observed cancellation. All18 separate child tasks edit the matrix to two states, observe two graph nodes, change speed by keyboard, reject invalid JSON and recover. The only cancellation is the initial local `/interactive-explanation/markov-chains/playground/` document, `net::ERR_ABORTED`, superseded by `playground/playground.html` with the preset query/hash. No wildcard allowance added. This classifies this exact event, not arbitrary future network failures or all child accessibility.
- Rigid-body: BLOCKED. A tight experiment changed only existing shell `window.__NUXT__.config.app.baseURL` from `./` to the served absolute directory pathname. Page-not-found disappeared, but the compiled home page hydrated over the lesson, removing all lesson sliders; hydration mismatch remained. Reverted completely. Retained source still emits Page not found and hydration errors in18/18 diagnostic samples. Existing slider access is not successful lesson initialization or simulation correctness. No engine mutation or error suppression was added.

## Source and provenance

Existing route provenance/parity records identify the retained Ableton, MLU and Setosa material. The only new adaptation is authored CSS wrapping and relocation of an existing navigation image; no new media, dependencies, license removals or content claims. Relevant parity JSON receives scoped W0 evidence and explicit corrections to prior rigid-body runtime-clean claims.

Nuxt supported seam consulted: https://nuxt.com/docs/3.x/api/configuration/nuxt-config and https://nuxt.com/docs/3.x/api/composables/use-runtime-config. `app.baseURL` is the router/application prefix; `buildAssetsDir` is relative to the base/CDN URL. It cannot by itself remap the archived article to the compiled home route while preserving this public URL. A supported source rebuild/route mapping remains required; no claim that changing compiled routes is authorized.

## Controlled evidence

Exclusive browser window was granted by the user; `pgrep` found no competing diagnostic/smoke/headless-shell processes before work. All browser commands ran sequentially. Chromium148.0.7778.96, default Playwright1.60 revision1223, same host/port4211, same `/interactive-explanation/` mount and no-store smoke server, same stored light/dark, viewports1400x1000/390x844/320x844, no additional throttling. Three fresh contexts per cell per side; fresh context is not cold OS/server cache.

`/tmp/opencode/w0-prereq-before.jsonl` and `w0-prereq-after.jsonl`: 54cells,162contexts each, existing unchanged diagnostic fixtures/readiness. Both retain full source inventories/hashes, raw Resource Timing, requests/errors and geometry. No reuse of a mismatched historical capture. The AFTER source inventory includes the final application CSS/HTML; later parity/research edits do not change served route inputs. Raw artifact SHA256/bytes are in `37-prerequisite-evidence.json`; preserve external artifacts because this is partial portability, not embedded raw navigation logs.

All54 paired DCL/load/transfer medians meet independent max(20%,250ms)/max(20%,250KiB) ceilings; count, median, min/max per cell are retained. Markov/rigid diagnostic error cells are not route acceptance despite those numeric budgets. Transfer is top-document completed Resource Timing, not child-complete or field performance. Resource request events remain raw. No payload class or new request intended: existing image moves; synth CSS adds11 lines. No field INP/LCP/CLS claim.

Narrow synth document widths change from384px BEFORE to320px AFTER: six routes x two themes x three contexts =36 samples per side. Every synth intrinsic geometry object is exactly identical BEFORE/AFTER at all six cells. Only runtime/footer height and derived aspect ratio change on mobile/narrow. Decision-tree desktop changes image position only; mobile/narrow also shifts downstream flow by the hero image line height. All protected intrinsic fields/count/order/backing/viewBox remain identical, verified by `37-prerequisite-verify.mjs`. No baseline successor written.

Independent `37-prerequisite-interactions.mjs` uses real keyboard/DOM navigation, not generic smoke success.162 AFTER records complete the scoped task; rigid-body records retain console errors and are explicitly not route-clean passes. BEFORE has48 failed access checks:36 synth narrow overflow assertions and12 decision-tree image checks; no fabricated BEFORE interaction timing for tasks blocked at access. Other comparable interaction medians/ranges are retained. The AFTER interaction capture preceded passive AFTER capture; neither overlapped browser work. BEFORE interaction source was the unchanged integration worktree; primary paired geometry/performance BEFORE used this isolated worktree itself.

Failed harness evidence retained: initial import selected CommonJS incorrectly and produced an empty output; corrected to existing `require` resolution. Run002 used a right-side-only synth menu predicate although desktop opens left, then timed out;74 records retained, not pooled into accepted timing. Corrected run003 checks runtime `show-toc` and Escape close, all162 tasks complete. No product change was made to satisfy a faulty predicate.

## Gates and remaining work

`37-prerequisite-smoke.json` retains exact commands/stdout/stderr/exits for all nine focused routes and full smoke, read-only successor002. All exit1: decision-tree's intentionally moved image, six synth mobile footer reflows, retained Markov/rigid geometry. Full smoke still stops at remember narrow. These do not suppress the independently executed162 interactions. Baseline owner must review this bounded reflow separately; final release remains blocked.

Additional narrow repro examined linear-regression339px, precision-recall324px, train-test-validation322px, memory-allocation328px. These are not the same footer/collapsed-link seam: compiled/math output, paragraph width-plus-margins, and runtime allocation labels respectively. No speculative fixes or broad shared overrides applied; retain separate owner investigation and paired evidence requirements.

Verification: configured syntax checks,370 Node tests and public-surface audit pass after edits; no separate typecheck/lint configured. `37-prerequisite-verify.mjs` checks54paired cells, protected intrinsic fields, exact synth geometry,162 task records and external raw hashes. Real-device/touch/audio output, assistive technology, all scientific interactions and cross-browser qualification are not certified. Rigid-body initialization and retained baseline/frontier failures remain actual blockers, not reasons to conceal evidence.

Re-run from repository root:

```bash
node .scratch/complete-route-experience/research/37-prerequisite-verify.mjs
node .scratch/complete-route-experience/research/37-prerequisite-interactions.mjs interactive-explanation /tmp/opencode/new-exclusive-interaction-output.jsonl
```
