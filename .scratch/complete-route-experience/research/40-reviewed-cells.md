# Evidence-only successor004

Base: `a5aa4a9fb13fd2bc2f6fbde6e766ab2384f77af6`, isolated `/tmp/opencode/w0-reviewed-cells`. Agent-authorized review, no human approval gate and no browser use. No route CSS/HTML, shared smoke, package01, successor003, or historical report edits.

## Reviewed cells

Six new geometry cells / 172 exact net paths:

- `decision-tree/mobile` and `decision-tree/narrow`: reconstruct the original reference from six identical historical7c71c99 and six full09505db samples, equal to six paired BEFORE samples. Then apply the separately captured authored image relocation and uniform downstream flow displacement. The 50x46 image is now onscreen; all downstream surface dimensions remain unchanged. BEFORE failed access is retained.
- `ableton-learning-synths-get-started/narrow` and `ableton-learning-synths-matching-envelopes/narrow`: same two-stage reconstruction, then the paired footer wrapping delta (height -22.09375 and derived fields only). Six BEFORE samples have 64px document overflow; six AFTER samples have zero. Every intrinsic surface is identical across the authored fix.
- `markov-chains/mobile` and `markov-chains/narrow`: exact historical/full/paired BEFORE/AFTER geometry equality. The original reference differs, not the captured source geometry. `markov-chains/desktop` is reviewed but already equal, so no baseline update.

Research37 supplies 18 successful actual Markov child tasks, six per viewport, plus exact raw-backed AFTER access records for all four authored cells. The passive Markov samples remain `failed`: each has exactly one network-classification error and one `net::ERR_ABORTED` for the initial local `/interactive-explanation/markov-chains/playground/` document, followed by a successful `playground/playground.html` response. All selected raw requests are same-origin, no HTTP >=400 or other failed requests/errors occur. This is classification of these captured events, not a wildcard or future network allowance. Research38 in this base is rigid-body source research, not the Markov evidence authority.

`40-reviewed-cells-review-004.json` retains every exact reconstruction, authored, and net path/value, artifact SHA256/bytes, actual source heads/digests/statuses, commands, and failed completion statuses. Historical34c9883 is verified and retained as MLU context, not misattributed to these cells. Paired AFTER was captured against dirty9782bff with precisely the CSS/HTML subsequently committed in83ae687; the verifier checks those two current file hashes against the raw inventory. All inherited performance values remain byte-value identical; historical timing is not promoted.

## Deferred, not erased

18 inherited geometry cells remain unchanged:

- Overflow without paired fixes: remember narrow26px; exponentiation narrow33px; public-private-keys narrow2px; reading-qr-codes-without-a-computer narrow35px; linear-regression narrow19px; precision-recall narrow4px; train-test-validation mobile/narrow2px; memory-allocation narrow8px; blockchain-101-combined-flow narrow2px.
- covid-19 desktop/mobile/narrow: 2px overflow plus no established historical/current equality.
- sim mobile/narrow: no qualified stable historical/current geometry.
- rigid-body-collisions desktop/mobile/narrow: retained Page not found/hydration initialization errors; no successful source rebuild. Slider access does not certify initialization.

Successor003's 11 authored cells remain inherited unchanged. No historical smoke failure becomes a pass; W0 release remains blocked. Failed research37 run002 remains hashed and excluded from accepted interaction pooling. External raw artifacts must remain available; this is not a self-contained raw archive.

## Reproduce without browser

From repository root:

```bash
node .scratch/complete-route-experience/research/40-reviewed-cells-verify.mjs --negative-check
node .scratch/complete-route-experience/research/40-reviewed-cells-verify.mjs --output /tmp/opencode/40-geometry-successor-004.json
```

The default command verifies the committed review against raw data. `--output` exclusively creates the full smoke-compatible baseline; it refuses existing files. Generated SHA256: `745446769a2b38b68e4e9750f13bab5b4c5ced6941143084d913a311f32a4173`. No duplicate full baseline is committed. Use that generated path with existing smoke `--baseline` only after browser exclusivity is released.

Validation: configured `npm run check`, all370 `npm run unit` tests, and `npm run audit` passed. No separate typecheck configured. Existing research37 negative checks reject missing/duplicate evidence and invalid metric samples; successor-specific negative checks reject wrong cancellation URL/error, extra HTTP/console failures, and protected backing-dimension changes. Standards/spec self-review kept the diff within research40 only; no parallel reviewer tool available.

Initial verification failed because this worktree lacked locked dependencies; installed with browser download disabled and scripts ignored. Initial `--write` collided with the imported003 verifier's exclusive output guard; no predecessor was overwritten. Renamed new creation flag to `--write-review`. A restrictive initial decision-tree predicate rejected the actual uniform downstream height/flow delta; the corrected predicate verifies exact permitted paths and unchanged surface sizes rather than discarding the failed check or loosening protected dimensions.
