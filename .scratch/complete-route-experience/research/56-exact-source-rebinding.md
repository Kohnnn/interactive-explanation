# 56 — Exact geometry source rebinding

## Corrected scope and evidence

The earlier claim in this document that the 62-cell registry remained an active approval was incorrect. Independent review withdrew every generic geometry admission. `tools/geometry-review.json` now has explicit `status: "withdrawn"` and zero cells; the historical raw and research artifacts remain evidence only. Exact same-source equality still admits unchanged base/head geometry, while any generic changed cell blocks. Rigid original-reference and fixed Sim comparisons remain independent and may admit only their own cells.

CI run `34442195321` rejected product digest `40d2e69bf997353eda8f00ed8e560a84164fbb7a9132fc0c1968cb0ecdf16de8`; source checkpoint `9618d8b9d6d03ec0ed27dd1c0c4c2775bbd9f3d3` has whole-product digest `e3d6a2c2feb9eab8bdd3594037bac93c2c8f0e472ee00738073145c923c3045d`. The prior claim that `542b16a..9618d8b` changed only package and metadata files was false: the complete Git diff contains 39 files and includes the Sim resize runtime plus capture, server, network, comparison, diagnostic, workflow, test, manifest, package, and evidence changes.

The historical review artifact contained 62 cells across Atlas plus 14 route slugs. Treating those cells as active approvals was incorrect. The active registry now contains zero cells and explicitly records withdrawal. Sim remains solely under fixed independent Sim admission; rigid-body-collisions remains solely under its original-reference admission.

## Exact geometry-relevant inventory

Each route cell binds every file under its route root, then recursively closes over fetched rendering dependencies from its `index.html`. The parser follows HTML `src`, resource-bearing `<link href>`, and `srcset`; CSS `@import` and `url(...)`; and JavaScript static imports/exports, literal dynamic imports, literal workers, and literal `new URL(..., import.meta.url)`. Root-relative and `/interactive-explanation/` references are normalized, repository traversal and missing files fail, and ordinary anchor navigation is not treated as a rendering fetch. Dynamically constructed paths that static parsing cannot discover are seeded from the exact retained raw request inventory. This includes the Ableton content, sound, preset, patch, model, and worker resources plus `train-test-validation/robot.cb528be1.svg`.

The closure now binds 436 unique concrete files across the 15 reviewed surfaces, with 705 surface/file memberships: Atlas 14; covid-19 102; decision-tree 51; exponentiation 25; public-private-keys 34; reading-qr-codes-without-a-computer 26; each Ableton route 59; linear-regression 52; train-test-validation 29; blockchain-101-combined-flow 18. Every surface blob-binds the complete `pages.json` and `routes.manifest.json` requested during capture in addition to the narrower rendering projections below. Every surface includes the recursively imported `shared/tokens.css`. Exponentiation includes its cross-root `ev/scripts/d3.js`, `ev/scripts/angular.js`, `ev/scripts/common.js`, and `ev/styles/style.css`, plus the latter stylesheet's `ev/resources/fonts/stix/STIX-Regular.otf` and `ev/img/setosa.png` dependencies.

Atlas additionally binds the exact rendered `pages.json` fields: slug, title, summary, intent, familyKey, topicTags, referenceUrl, addedDate, docsUrl, and suggestedNextSlug for every entry. Route cells additionally bind navigation slug/title/suggestedNextSlug for every manifest entry and their own learning, themeOwnership, primarySurface, and runtimeSurface fields. Network policy and interaction probes are excluded because capture uses them for diagnostics/readiness enforcement but they do not render geometry; their changes remain subject to separate full-checkout, resource, functional, and tool contracts. Missing dependencies, changed bytes, selector drift, Atlas-rendered metadata drift, duplicate source entries, or traversal fail. `sim/index.html`, Musicmap route assets, package scripts, package locks, docs, workflows, research, and unrelated tools are not geometry dependencies for these cells.

Base and head per-slug dependency hashes are recorded in `tools/geometry-review.json`. The geometry-relevant projections at captured head `542b16ada9dfcac0edb1e6ce9f3f29e2e1122d23` are byte-identical at admitted source checkpoint `9618d8b9d6d03ec0ed27dd1c0c4c2775bbd9f3d3`. Existing qualification still verifies the complete checkout against its exact requested SHA, Git blobs, modes, paths, cleanliness, and full `sourceIdentity` before capture, then verifies unchanged full identity after capture. This registry does not weaken that global check.

## Complete `542b16a..9618d8b` classification

Geometry-relevant product changes: none. Independently changed product/config files are `sim/index.html` (runtime resize redraw), `pages.json` and `routes.manifest.json` (Musicmap deferred-host policy only), `package.json` (check script only), and `docs/sim/parity.json`. Sim is not one of the 62 reviewed cells; metadata network arrays and package scripts are excluded only from this geometry projection, not from global/resource/functional verification.

Capture and diagnostic changes are `tools/diagnose-baseline.mjs`, `tools/smoke-bundle.mjs`, `tools/smoke/server.mjs`, and new `tools/network-handoff.mjs`. Replay/qualification changes are `tools/qualify-pr.mjs`, new `tools/geometry-output.mjs`, `tools/geometry-review.mjs`, `tools/geometry-review.json`, `tools/functional-qualified.mjs`, `tools/resource-review.mjs`, and `tools/resource-review.json`. Independent Sim/rigid/Markov tools are `tools/reproduce-sim-resize.mjs`, `tools/sim-reference.mjs`, `tools/rigid-body-browser.mjs`, `tools/rigid-original-admission.json`, and `tools/markov-browser.mjs`. Metadata tooling changed in `tools/sync-route-metadata.mjs`. Tests changed or were added for diagnose-baseline, geometry-output, geometry-review, manifest-validation, network-handoff, qualify-pr, rigid-original-admission, and smoke-server. Pipeline/policy/evidence changes are `.github/workflows/ci.yml`, `AGENTS.md`, `.scratch/complete-route-experience/spec.md`, `.scratch/complete-route-experience/implementation/01-restore-release-evidence.md`, research 49/50/51/52/54.

## Measurement and artifact contract

The raw artifact remains bound to captured head `542b16a`, not relabeled as captured at `9618d8b`. Historical capture blobs are pinned separately: `tools/diagnose-baseline.mjs` `4b387489f42602bf653981e8c391fe1e4258132b2bc10168e1387b417178d950`; `tools/experience-baseline.mjs` `9f8edb47ba512f9ac42028ac2ef8be7da3ad0123e73e0978cc23d259dbced468`; `tools/smoke/server.mjs` `5ccc906a3ebfe15cef5e86e7eb04805edecd7503815295241aff46feb8c57e16`; `tools/smoke-bundle.mjs` `560719ce93ddfc9394c38e74e3d91faa7e091fe0a19c735cb7241e91cb43bb00`. Whole replay blobs for `tools/qualify-pr.mjs`, `tools/geometry-review.mjs`, and `tools/network-handoff.mjs` identify the reviewed `9618d8b` checkpoint; current execution separately requires exact function hashes for geometry diffing, validation, comparison, source projection, function extraction, contract verification, and admission.

The exact source hashes of `waitForDocumentLayout`, `waitForManifestRouteReady`, `measureRuntimeSurface`, and `scrollPrimarySurfaceIntoView` are byte-identical between `542b16a` and `9618d8b` and are recorded individually. Whole capture tools are not claimed unchanged: network event collection, URL handling, server switching, scheduling, readiness timestamp logging, and qualification behavior changed. Fresh current captures produce richer network evidence and are not represented as historical raw captures; only read-only replay of the unchanged raw geometry values uses this registry.

The historical review artifact recorded 62 reviewed cells and 1,590 path/before/after leaves with SHA-256 `23054c887be2a6a0df0fc18822246811b711efe69ccdbff72f97f75c37f0f776`. That artifact is preserved in research evidence but is not the active registry. `tools/geometry-review.json` now has zero cells and explicit withdrawn status. Raw journal SHA-256 remains `951a010e8965261cb10fb5162608dd05f586af0e1585abfed031646d9f071926`; raw cell count remains 504. CI does not require that 284 MiB journal. `tools/geometry-runtime-requests.json` commits its 239 normalized local paths, grouped by the 15 reviewed surfaces and exact cells, with run `34390287521`, raw hash, per-surface and aggregate hashes, and base/head Git blob IDs and modes. Qualification validates every committed path against each checkout's immutable Git tree before browser launch, then requires every normalized local path observed by each fresh reviewed capture to belong to that surface's static closure or committed inventory. Queries and fragments do not affect local path identity; an omitted retained path, extra current path, route/cell mismatch, missing file, changed blob, or changed mode fails. Six Ableton surfaces additionally create transient `blob:` URLs, recorded explicitly but not repository files. The optional forensic command `node tools/verify-geometry-runtime-requests.mjs . <qualification.jsonl>` rechecks the external artifact hash and reconstructs the committed inventory offline. Static parsing intentionally cannot prove computed runtime URLs, CSS generated by script, or requests absent from both captures; retained runtime paths cover prior observations and the fresh-capture subset gate rejects newly observed local dependencies. No cell, leaf, value, raw hash, approval path, mask, tolerance, or legacy baseline changed. No browser was run for this contract update. W0 remains blocked pending independent review and CI.
