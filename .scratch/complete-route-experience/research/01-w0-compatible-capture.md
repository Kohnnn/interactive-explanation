# W0 compatible-browser capture and bounded successor

## Result

Measured immutable integration source `09505db333598ac50553973b638872f039c4e7d4`, Chromium 148.0.7778.96, Playwright 1.60.0, default revision 1223. Full capture completed 504 cells and 1512 sequential fresh contexts, with source inventory unchanged. 1476 samples were measured without diagnostic errors; 36 retained errors belong to Markov chains and rigid-body collisions. Capture is not acceptance. No W1 advancement.

Focused polygons/decision-tree/covid capture: 54 contexts. Historical introducing source `7c71c992be944895438ec2467d20f18a7ccd4a63` was reconstructed as a detached worktree and measured with the same integration diagnostic seams: polygons/decision-tree, 36 contexts. All twelve theme/viewport comparisons have identical full intrinsic geometry between historical and integration sources. No claim that their source trees or performance payloads are identical.

Full capture completed at 2026-09-09T04:40:23.470Z. Focused capture completed at 04:09:59.068Z. Raw identity records contain exact source file hashes, browser catalog, command, timing and cache conditions. The full capture ran before historical follow-up; no browser workloads overlapped. Raw source inventory digest was c9ceecbd59f2a0df056035807e00fd4e3be69baea64060cde4436b2c1e64e9c4.

## Approved reference reconstruction, not route acceptance

New separate baseline: `01-w0-geometry-successor-001.json`. Exact 79 approved JSON-pointer differences: `01-w0-geometry-successor-review-001.json`. Inherited tools/experience-baselines.json is unchanged. All 166 light/dark performance entries and all other geometry cells remain exactly equal to inherited values.

Approved cells: polygons mobile and narrow; decision-tree desktop. Each has 18 identical full geometry samples across historical/current/full captures and both themes. Intrinsic order, identity, backing width/height, SVG viewBox and iframe titles remain identical to inherited references. Polygons changes are vertical flow offsets, outer height and derived aspect ratio. Decision-tree desktop additionally has two SVG CSS-height changes; current historical-source and integration output agree exactly, with backing/viewBox unchanged. These are accepted only as compatible-environment reference reconstruction, not new renderer adaptation, visual certification or pointer interaction approval.

Classification: original recording unqualified; unchanged source reproducible in compatible environment. Original recording environment remains unknown; this is not a proven browser bug. Agent review is this specialist's source/data review, not independent human or second-agent visual review. No screenshots or interaction equivalence are claimed.

Excluded: decision-tree mobile/narrow because the first image is entirely left of viewport (left -58.8125, right -8.8125 at 320px), also already offscreen in inherited geometry. This needs authored-content ownership review, not normalization. Covid has stable larger heights (+37.390625 desktop, +305.9375 mobile, +428.3125 narrow), but no matched historical adapted-source comparison and 2px overflow; no successor approved. No other route geometry is accepted automatically. Sim mobile/narrow full geometry varies across repeats. Seventy route slugs have at least one inherited mismatch, exact cells retained in full summary.

## Unchanged gate reruns

Full smoke with successor and --verbose: failed at ballot desktop bottom. Trust and polygons completed first-loop geometry, compatibility and inherited performance checks. Atlas, later route/docs and interactions were not reached; no complete route acceptance.

Strict command: exact 59 historical migration route filters, --experience, successor, --verbose. Exit 1 at decision-tree mobile geometry. Desktop reference passed; strict state dispatch was not reached. Full command and child exit retained in `01-w0-strict-successor-001.log`. Full failure log is `01-w0-full-smoke-successor-001.log`.

Next evidence frontier: paired historical ballot geometry, then individually reviewed reference reconstruction where access and intrinsic boundaries are demonstrably preserved. Next bounded prerequisite route review: decision-tree offscreen image/oversized continuation layout, rigid-body subpath initialization, six synth narrow layouts. These are not authorization for opaque engine rewrites. Package04 owns decision-tree, package05 synths, package06 ballot/rigid-body and other opaque boundaries.

## W0 signal dispositions

- Full ready-state capture finds 42 overflow cells, not assumed identical to historical membership. Maximum 64px on all six synth routes at 320px in both themes; prior Chromium151 figure was 68px and is not a comparable improvement claim. Exact membership and sample deltas are retained in full summary. Other values: remember26, coming-out40, exponentiation33, QR35, linear-regression19, memory-allocation8; smaller 2–4px cases remain recorded, not blanket exempted. Route-family owners investigate content reachability; no overflow hiding or renderer shrink used.
- Musicmap primary and continuation visible after readiness, 320px both themes. Anxiety primary and continuation visible after readiness, 320px both themes. Chords/melodies continuations visible in DOM flow after readiness, 320px both themes; they are far below opening viewport, not missing. These targeted probes do not certify every state, activation or full three-sample readiness matrix.
- Markov chains: 18 failed initial child document requests, net::ERR_ABORTED. Follow-up frame probe in both themes reaches playground/playground.html, readyState complete, named playground and controls. This supports superseded navigation classification rather than blank-child failure, but no child interaction acceptance or wildcard network allowance was introduced. Raw cancellation remains a failed diagnostic sample pending complete child task probe; package06 owns that boundary.
- Rigid-body: 18 samples retain Nuxt Page not found at the deployed subpath and hydration mismatch console errors. Lesson DOM remains visible, which does not excuse initialization failure. No local HTTP >=400 response recorded in full capture; historical favicon request not reproduced by this headless run, not declared fixed. Package06 owns the authored asset/subpath seam investigation.
- Generic main absence is not used as a defect; manifest primary/runtime selectors remain authoritative.

## Evidence and limitations

Raw artifacts are retained under `/tmp/opencode/w0-baseline-09505db-evidence/`; exact filenames, byte sizes and SHA-256 are in `01-w0-raw-artifacts-09505db.json`. The large raw JSONL captures remain external; summaries, exact geometry comparisons, successor/report, full/strict failure logs and targeted signal output are committed. This is explicit partial portability, not a claim that raw samples are embedded in Git. Keep the external directory until evidence is archived elsewhere.

Commands: `SMOKE_PORT=4199 timeout 1800s node interactive-explanation/tools/diagnose-baseline.mjs --root <identified-source>/interactive-explanation --output <new-path> --route polygons --route decision-tree` (add --route covid-19 for integration focused run). Full: same command without route filters and timeout 14400s. Every output path was new. Full smoke used timeout3600 and --baseline pointing to the new external successor. Strict launcher records the exact expanded 59 filters and exit status.

Static verification on the follow-up worktree: configured syntax checks passed, all 370 Node tests passed, audit passed, and successor self-check passed. The first self-check failed only because report paths were ordered by review sequence while baseline traversal was alphabetical; sorting both path lists corrected the check without changing baseline or review values. That failed tooling result remains in session output.

No runtime/application change, tolerance change, automatic baseline recording, performance refresh or failed-sample dropping occurred. Parent-only raw Resource Timing excludes child entries and may truncate; request events supplement counts. Three fresh contexts do not imply cold OS cache. Timing ranges and all samples remain available, not paired performance acceptance. Stored-theme captures do not establish system fallback, keyboard/touch, storage denial, reduced motion, assistive technology, real devices, cross-browser behavior, field metrics or learning outcomes. W0 remains in progress because actual defects and mandatory smoke/strict acceptance remain unresolved; missing old metadata alone is no longer the generic blocker.
