# Research 39 — owned narrow reflow

## Pre-edit contract

Source: isolated `/tmp/opencode/w0-owned-reflow`, branch `implement/w0-owned-reflow`, HEAD `a5aa4a9fb13fd2bc2f6fbde6e766ab2384f77af6`. Source preimages and all file SHA256s are retained by `/tmp/opencode/39-owned-before.jsonl` identity records. Application unchanged during BEFORE. Existing original Playwright 1.60.0 installation linked after initial module-resolution failure; no installation or dependency manifest changes.

BEFORE diagnostic: seven routes × three viewports (1400×1000, 390×844, 320×844) × stored light/dark × three fresh contexts, sequential default Chromium. Existing diagnose-baseline readiness/server/network/geometry/performance fixtures. Narrow independent probe reproduced 33/2/19/4/2/8/2px respectively. Raw precise elements: `/tmp/opencode/39-owned-repro-isolated.json`; earlier broad probes retained.

Expected authored deltas, declared before edits:

- exponentiation: wrap unbreakable 48px legacy title within 280px prose. Title/ancestor height and subsequent vertical positions may change at 320px. Existing 960px growth/virus surfaces remain unchanged; name and keyboard-enable their existing scroll containers.
- public-private-keys and blockchain-101-combined-flow: route-local narrow lab hero single-column override, correcting higher-specificity desktop grid rule. Hero children reflow; intrinsic demos unchanged. The keys navbar title also needs native wrapping, not offscreen clipping.
- linear-regression: wrap legacy intro title; bounded native horizontal access for oversized equations, not chart scaling. Reflow changes title height and downstream positions; formula layout widths remain intrinsic. Add names/focusability at authored adapter seam if required for dynamically mounted formula containers.
- precision-recall: vertically place existing F1 chart and legend at narrow widths so 290px chart and 64px legend retain dimensions without overlapping. No engine or SVG dimension changes.
- train-test-validation: bound padded scrolly prose border boxes to their containing width. Paragraph height/downstream placement may change; graphics unchanged.
- memory-allocation: wrap the existing three 7rem timeline labels within their generated row. Preserve labels, canvas/backing size, pointer mapping and timeline behavior. Authored CSS selector uses the existing step-text identity, not bundled runtime edits.

No new resource requests expected for inline CSS/adapter additions. DCL/load increase ceiling independently max(20% of corresponding BEFORE median, 250ms); transfer max(20%, 250KiB). Raw resource entries retained for top-resource review. No shared smoke, package01, baseline or license changes. No performance acceptance until paired AFTER and variance review.

## Final outcome

Actual scoped application fixes: eight files, 67 additions/7 deletions before parity notes. No shared files, package01, baselines, minified engines, dependencies, licenses, attribution or source URLs changed. Root-only keys changes do not certify signing subroutes.

Final narrow widths (each three samples, both themes): exponentiation 353→320; public-private-keys 322→320; linear-regression 339→320; precision-recall 324→321; train-test-validation 322→320; memory-allocation 328→320; blockchain-101-combined-flow 322→320. Precision retains 1px within the existing gate tolerance; no measured element extends beyond that tolerance outside intentional scrolling. No global clipping or scaling was added.

The initially attempted stacked F1 chart/legend reflow was rejected: its resize-listener height fed back and changed legend intrinsic size. Final source instead adds overflow-x:auto to the existing bounded F1 container, with a name and tab stop. Original chart, legend and resize-listener dimensions are preserved. All 42 final paired cells have exactly stable geometry, no intrinsic width/height/backing/viewBox/identity or pointer/transform changes, and no passive runtime/network errors. Authored container heights and downstream positions change as declared; exact paths remain in final evidence, not an approved baseline.

Final paired capture uses the existing diagnose capture fixture, all seven routes × 1400×1000/390×844/320×844 × stored light/dark × three fresh contexts per side. BEFORE is git-archived HEAD, verified byte-for-byte against every original source hash. Alternates BEFORE/AFTER per sample with one browser and one server at a time on port4221; source unchanged per side. Browser default Playwright1.60 Chromium148, Linux, Node24.20.0. No OS cache or production-network claim.

Initial serial candidate capture exceeded timing ceilings in ten cells amid system load (observed load averages 27.18/31.80/29.44); all samples retained. Complete alternating repeat passed budgets but exposed rejected F1 instability. Final complete alternating capture passes all independent DCL/load/transfer budgets across 42 cells with medians/ranges retained; resource delta exactly zero everywhere, maximum transfer increase732 bytes. Final top-five resource duration/transfer records per cell are in `39-owned-reflow-final-evidence.json`: existing fonts dominate most routes; exponentiation also loads Angular and the existing shell. No font/engine optimization or field CWV claim.

Independent interactions: 126 BEFORE and126 final AFTER contexts. Final108 pass,18 fail, all failures train-test-validation. Tests actually press keyboard Enter/ArrowRight, observe keypair/readout/feature state, and verify named region keyboard scrolling plus end reachability. No private key material retained. Exponentiation, keys, linear regression, precision/recall, memory and blockchain handoff each pass18/18. Memory check proves keyboard controls activate/value changes, not all allocator outputs. TTV feature button changes active state but raises `Cannot read properties of undefined (reading 'includes')` from `js.6c47f979.js:729:21936` during the action, also reproduced on immutable BEFORE. Required opaque engine edit is outside authorization; no catch, allowlist or masking added. Stack traces retained in interaction evidence.

Final static gates: check0, audit0, unit0 (370/370). Earlier canonical-CSS-order unit failure corrected by placing inline styles before shared CSS, without weakening tests. Initial fourteen focused startup failures used a wrong relative baseline path and are retained; corrected fourteen normal/strict invocations actually executed and all stop at successor003 geometry, before later strict dispatch. Exact final failures, each normal and strict:

| Route | First failing comparison |
| --- | --- |
| exponentiation | mobile runtime bottom >1px |
| public-private-keys | mobile runtime bottom >1px |
| linear-regression | desktop runtime bottom >1px |
| precision-recall | narrow runtime bottom >1px |
| train-test-validation | desktop img:9 right >1px |
| memory-allocation | narrow runtime top >1px |
| blockchain-101-combined-flow | mobile runtime bottom >1px |

These are retained gate failures, not fourteen full passes or permission to rewrite baselines. Final same-environment paths are supplied for the separate baseline owner's review. Combined full/59 strict not rerun by this scoped owner; no release or W1 acceptance. Cross-browser, real device/touch, screen-reader and exhaustive pointer/science coverage remain unmeasured.

## Reproduction and artifacts

From this worktree:

- `node .scratch/complete-route-experience/research/39-owned-reflow-probe.mjs interactive-explanation <new-output>`: narrow red-capable fit/element probe. BEFORE rejects all seven historical overflows; final uses unchanged1px tolerance.
- `SMOKE_PORT=4221 node .scratch/complete-route-experience/research/39-owned-reflow-paired.mjs <new-prefix>`: full alternating capture. Requires immutable archive at `/tmp/opencode/39-owned-preimage/interactive-explanation` plus original identity JSONL. Archive was created with `git archive a5aa4a9 interactive-explanation`, dependency link to verified original installation only.
- `node .scratch/complete-route-experience/research/39-owned-reflow-compare.mjs /tmp/opencode/39-owned-final <new-output>`: checks counts, exact sample IDs, stability, intrinsic fields and budgets; no baseline output.
- `node .scratch/complete-route-experience/research/39-owned-reflow-interactions.mjs <site-root> <new-output>`: all126 keyboard contexts, failures retained.
- `node .scratch/complete-route-experience/research/39-owned-reflow-gates.mjs <new-output>`: static gates and14 independent successor003 normal/strict invocations.

Portable reports contain exact cell paths, samples/metric distributions, runtime stacks, gate commands/stdout/stderr/exits and artifact hashes. Large raw journals remain in `/tmp/opencode/39-owned-*.jsonl`; SHA256s and byte counts are retained by interaction/final evidence. Candidate reports are explicitly historical rejected evidence, not current acceptance. No independent-agent review facility was available; this is implementer review. Rollback only owned route/parity hunks and research39 files. No merge/push/deployment.

