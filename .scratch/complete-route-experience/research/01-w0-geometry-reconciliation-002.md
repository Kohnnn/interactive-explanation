# W0 geometry reconciliation and complete filtered gate frontier

## Scope and identity

Branch implement/complete-route-geometry starts at 9782bff. Application source is byte-identical in Git to 09505db333598ac50553973b638872f039c4e7d4; no route, shared runtime, smoke assertion, or baseline tool was edited. Existing full BEFORE evidence is reused, not overwritten. Parent package01 is updated here; prerequisite agent owns route changes elsewhere.

Browser: default Playwright1.60 Chromium148.0.7778.96 revision1223. Historical followups and gate commands ran sequentially on port4207 within this specialist, but another agent may have run focused browser tests. Therefore all new timing evidence is contention-unqualified, not a controlled performance comparison. Inherited performance values were not changed. Existing BEFORE timing retains its previously stated conditions.

## Historical comparison completed across the matrix

Historical introducing source 7c71c99 was captured for all83+Atlas: 504cells/1512contexts, completed 2026-09-09T05:47:14.833Z, unchanged source inventory. Missing historical canonical gitlinks fail explicitly; the fixture was not silently populated from current source. Runtime-error cells remain errors. This replaces the earlier two-route-only provenance investigation.

Four routes with later geometry provenance (linear-regression, logistic-regression, precision-recall, roc-auc) were additionally captured from 34c9883 in 72contexts. Their current full geometry matches this later historical source exactly; the differences against 7c71c99 were source-version differences, not attributed to browser behavior.

All 249 current route geometry cells were classified. 187 differ at exact JSON leaf level from inherited values, including sub-tolerance differences. Classification preserves exact historical/current deltas, stable/error/overflow status and protected-field checks. This is broader than the previous 70-slug assertion failure count, because exact leaf differences can pass the existing tolerance.

## Reviewed successor002

`01-w0-geometry-successor-002.json` is a NEW complete baseline, selected only explicitly with --baseline. Original baseline and successor001 remain unchanged. Review: `01-w0-geometry-review-002.json`, 163cells and 13805 exact JSON-pointer/value deltas, including the original79 paths. All166 light/dark performance entries unchanged.

Each approved cell has six current samples and six historical samples (three fresh contexts in each stored theme), no diagnostic errors, exact full geometry equality across all12 samples, no document overflow above1px, and unchanged intrinsic count/order/key/tag, backing width/height, viewBox and iframe titles. Approved changes are confined to CSS dimensions, runtime/intrinsic rectangles and derived aspect ratios. These include text-flow displacements, text-derived inline SVG extents and stable renderer CSS rectangles. There is no current-source renderer mutation to justify, no invented synthetic median geometry and no relaxed assertion.

Agent review classifies these exact cells as original recording unqualified; unchanged historical/current source output reproducible in compatible environment. Not proof of a browser bug. This is reference reconstruction, not whole-route accessibility/interaction certification. Every resulting cell was exercised through unchanged smoke in the per-route sweep; 47 strict route commands additionally completed their available stored/system theme and accessibility probes. Inherited conditions not exercised by those probes remain explicit limits.

Twenty-four changed cells are withheld: every overflowing changed cell, decision-tree mobile/narrow despite historical equality, sim mobile/narrow instability, covid lack of equivalent historical adapted input plus2px overflow, Markov cancellation-error cells, rigid-body initialization-error cells. All withheld values remain inherited. No synth narrow overflow or inaccessible decision-tree image was normalized. Unchanged geometry cells with overflow remain defects too; a matching inherited rectangle is not an exemption.

## All gate commands executed

144 unchanged CLI commands completed between 05:51:30.004Z and06:37:54.914Z:

- Full smoke: exit1, now reaches remember narrow geometry. Trust, polygons, ballot, crowds, loopy and neurons complete initial geometry/compatibility/performance pipeline first. Full suite is NOT passed.
- Exact combined59-route strict: exit1 at decision-tree mobile geometry, before strict state dispatch for that route. NOT passed.
- All83 individual route commands: 67exit0,16exit1. Each successful invocation includes route/docs checks, Atlas, wayfinding and its dispatched interaction routines. Atlas was thus actually exercised, unlike the earlier fail-fast-only frontier.
- All59 individual strict route commands: 47exit0,12exit1. No skipped selected route, no process timeout or signal reported. These are observed command outcomes, not contention-qualified performance acceptance or a combined full pass.

Exact commands/start/end/exit/first failures: `01-w0-geometry-gate-results-002.jsonl`. All144 stdout/stderr logs are embedded without truncation in `01-w0-geometry-gate-logs-002.json`; hashes and passing/failed slug lists in `01-w0-geometry-gate-summary-002.json`.

### Remaining16 route failures

| Route | First failing cell | Disposition / owner |
| --- | --- | --- |
| remember | narrow | Reproduces historical geometry but26px overflow; opaque-route boundary owner |
| covid-19 | desktop | Historical adapted input absent;2px overflow; canonical-source/access owner |
| sim | mobile | Repeat-unstable position; readiness/animation fixture investigation before successor |
| decision-tree | mobile | Historical equality but offscreen image/access review; prerequisite agent |
| markov-chains | mobile | Stable-looking geometry retained behind cancellation classification; child lifecycle owner |
| exponentiation | narrow | Historical equality but33px overflow; math-route owner |
| public-private-keys | narrow | Historical equality but2px overflow; crypto-route owner |
| reading-qr-codes-without-a-computer | narrow | Historical equality but35px overflow; opaque-route boundary owner |
| ableton-learning-synths-get-started | narrow |64px overflow; prerequisite agent |
| ableton-learning-synths-matching-envelopes | narrow |64px overflow; prerequisite agent |
| linear-regression | narrow | Historical34c equality but19px overflow; prerequisite agent |
| precision-recall | narrow | Historical34c equality but4px overflow; prerequisite agent |
| train-test-validation | mobile | Historical equality but2px overflow; prerequisite agent |
| memory-allocation | narrow | Historical equality but8px overflow; supported shell owner |
| rigid-body-collisions | desktop | Nuxt subpath/hydration errors; prerequisite agent supported seam |
| blockchain-101-combined-flow | narrow | Historical equality but2px overflow; guided-path owner |

The12 strict failures are the same list excluding remember, covid-19, sim and reading-qr-codes-without-a-computer (not in selected59). No new performance first-failure occurred in these runs. Four other synth strict commands return0 even though passive capture found narrow document overflow: existing checks are not a waiver for that recorded signal.

## Next action, without repeating the full passive BEFORE

Merge/review bounded prerequisite changes separately. Capture only affected AFTER cells using same fixtures, with a quiet timing window for any performance acceptance. Review authored reflow paths separately from this historical reconstruction and retain backing/pointer constraints. Re-run the16 failed route commands and12 failed strict commands with the resulting reviewed successor, then combined full/strict. Remaining opaque overflow and sim/Markov fixture investigations need explicit owners; do not hide them behind a fresh all-route baseline. No W1/full-release readiness yet.

Raw source identities and historical samples are external at /tmp/opencode/w0-geometry-9782bff-evidence, with inventory/hashes committed in `01-w0-geometry-artifacts-002.json`. Existing BEFORE is /tmp/opencode/w0-baseline-09505db-evidence/full-001.jsonl. Preserve both directories. New source content untouched during gate execution, verified by empty application diff against09505db. Raw historical complete records report unchanged inventory. Portable source reconstruction uses detached Git worktrees at7c71c99 and34c9883 and the existing diagnostic --root selector.

Verification: configured syntax checks, all370 Node unit tests, policy audit and the runnable exact-successor verifier passed. The verifier checks all13805 paths,163 cells, protected intrinsic fields, unchanged performance, deferred cells and83/59 outcome totals.

Readiness: reference reconstruction and per-route gate enumeration are now complete for this source; W0 acceptance remains blocked by the explicit16-route frontier and remaining qualification obligations. Real-device/AT/cross-browser, storage denial, reduced motion, deep child interactions and controlled AFTER performance are not silently certified. No permanent collect-all framework was added.
