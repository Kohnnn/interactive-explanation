# 07 Final qualification

Status: ready-for-agent
Spec: [Master specification](../spec.md)
Blocked by: [06 Opaque Route slices](06-opaque-route-slices.md)
Execution: not started
Wave: W5 — zero new implementation assignments; qualify all 83 Routes plus Atlas

## Scope and first-session boundary

Begin after package 06, transitively every earlier package, passes its gates. This supersedes retired closure 29, not a claim that its implementation finished.
First slice: reconcile checkpoint coverage and source identity, then execute full compatible-browser smoke. Do not treat one run or one session as all modality qualification.
Validate exact manifest/snapshot/package membership: packages 03–06 partition 83 Routes as 3/31/22/27 with no duplicate; Atlas belongs only to 02.
Account for every canonical family, theme ownership, navigation mode, primary/runtime surface, continuation and declared network/interaction boundary.
Review all package logs, unresolved signal dispositions, known baseline failures and retained historical references; absence of evidence remains blocked rather than accepted.

## Observable release gates

- Full 83-Route smoke plus Atlas must actually pass in the compatible environment. Run strict experience for every migrated Route and retain actual selection/results; no representative-family substitution.
- Complete every applicable master component/state/theme/input cell, including saved/system theme, keyboard/pointer/touch, reduced motion, zoom/focus, feedback/reset, loading/error, audio and child boundaries.
- Real-device, cross-browser, assistive-technology and child-state obligations require qualified evidence; unavailable modalities block full-experience release qualification. Do not claim whole-site AA from token contrast or shell probes.
- Zero new unexpected runtime exceptions, console/local HTTP/network failures; zero unexplained geometry, masked clipping, lost data, broken URL or altered intrinsic engine behavior.
- Paired before/after measurements use the same provisioned browser/environment and three fresh contexts per side, with median/range and explicit cache/readiness limitations. No direct comparison to mismatched Chrome 151 snapshots.
- DCL/load deltas stay within max(20% of before,250ms); transfer within max(20% of before,250KiB). Declare all resource deltas; unsupported/missing measurements are not zero or passes.
- Approved authored reflow has separate reviewed successor baselines retaining history; no blanket refresh to hide regressions.
- Accuracy, attribution/license obligations and honest retained/adapted/new provenance hold. Bias–variance assumptions, toy blockchain limits and major-scale spelling remain correct.
- Preserve one local Suggested Next Route, Guided Path-only Progress, existing user data/theme persistence and opaque ownership; no new framework, fonts, analytics or runtime dependency.
- Report visual/content judgments as agent review and learning value as hypothesis. No invented human approval, learning result, field CWV or production-device certification.

## Commands and output

Run from parent `interactive-note/` root:
`npm --prefix interactive-explanation run check`
`npm --prefix interactive-explanation run unit`
`node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route <migrated-slug> --experience`
Repeat exact Route flags across the full migrated set; supplement existing seams for all applicable manual/device states. No new testing framework or implicit synchronization command.
Deliver a qualification report with exact passed/failed/blocked/unsupported scope, source/environment and raw evidence references, budgets, resource changes, baseline history and remaining obligations.
Qualification may report ready only when every mandatory gate is fulfilled; otherwise release stays BLOCKED with owners and next actions. Even qualification success does not deploy or commit.

## Checkpoint, resume and failure handling

Append completed/remaining checks and exact Route sets, input identity, commands/evidence, outstanding modalities, decisions and next safe action here each session.
On failure, return the smallest affected owned slice to its package with reproduction evidence; do not reopen the entire wave or erase unrelated dirty work.
Retain failed runs and baseline history. Rollback only owned hunks with preimages and coordination for overlap; protect local user data and licenses.
Routine matching remediation follows existing package scope under an implementation request; material out-of-scope departures require human decision. Installation, commits and deployment remain separately authorized.

## Live port171 browser checkpoint — 2026-09-13

User authorized browser verification and minimal proven functional/accessibility fixes in `/tmp/opencode/complete-route-pr`, with historical timing and unavailable human review nonblocking for the requested redesign. Functional failures remain hard. This supersedes the earlier scheduling stop, not a claim of complete qualification. No commits, push, deployment, baseline refresh, admission hash refresh or parent source-contract edits were performed. Browser runs were sequential, Chromium `148.0.7778.96`, Node `v24.20.0`, Linux.

### Actual results

- Dedicated existing `node tools/rigid-body-browser.mjs .` ran first and passed: six viewport/theme cells, 18 fresh repeated contexts and four fallback contexts. Checked collision numeric results, keyboard/pointer/touch, reset, invalid input, theme/reload, local requests, focus and targets. Output is retained in the session transcript; no paired-performance acceptance is implied.
- Unchanged `node tools/smoke-bundle.mjs .` ran before fixes and failed at its historical geometry gate: `Error: polygons desktop geometry baseline runtime bottom shifted by more than 1 CSS px`, `assertRuntimeGeometry` at line 1162, `measureRouteGeometry` at 1199, `main` at 9773. Later smoke tasks did not run. This failure was not used as a substitute for functional testing.
- Added `tools/functional-sweep.mjs` because `tools/functional-qualified.mjs` requires admitted generated geometry and cannot separate this scope. Only exports were added to `tools/smoke-bundle.mjs`; existing assertions and main runner are unchanged. The new script reuses manifest readiness, three qualification viewports, saved theme initialization, Teoria sample fixture, runtime/network/console policies, continuation, read-only runtime, accessibility, generated/native controls and declared child contracts. Native Node assertions reject failures; JSONL output is exclusive and durable.
- Final full command: `node tools/functional-sweep.mjs . /tmp/opencode/port171-functional-final.jsonl`. Completed all **504/504** cells (83 routes plus Atlas; 1400×1000, 390×844, 320×844; light/dark): **492 passed, 12 failed**. Route-only result **486/498**; Atlas **6/6**. All 504 passed readiness, initial theme, viewport and runtime/network/console phases. This is functional shell/read-only/native-contract coverage, not every engine action on every route or strict59 experience acceptance.
- Exact remaining 12 failures: `remember` and `covid-19`, each desktop/mobile/narrow × light/dark, phase `navigation-controls-child-contract`. Desktop: `page.waitForSelector: Timeout 15000ms exceeded`, waiting for `.story-rail__nav a` visible. Mobile/narrow: same timeout waiting for `.story-mobile-bar__toggle` visible. Browser DOM confirms no rail/bar exists; metadata says `generated` but has no chapter entries and pages expose no mounted chapter navigation. No runtime/console/network exception accompanied these failures. Origin as an authored regression was not established; no invented chapter list or weakened navigation contract was applied. Parent must resolve intended chapter/navigation ownership before treating these cells as passed.
- Atlas filters/history fixture passed all six invocations: intent via keyboard, topic, combined filters, Back/Forward, reset, URL-restored sort and continuation cards. Theme toggling/storage and a fresh context seeded from actual storage verified persistence in all six cells. The existing Atlas fixture itself switches to 390px at its end; narrow initial viewport and theme checks ran separately, not a claim that every internal Atlas action stayed narrow.
- Five orientation disclosures (`trust`, `loopy`, `sim`, `wbwwb`, `coming-out-simulator-2014`) passed **30/30** final cells: Enter opens, pointer closes, paragraph visible, summary and continuation pointer hit-testing, no detected covered visible native main controls, viewport fit. Occlusion checks cover the observed DOM/control state, not all future game states or arbitrary canvas hit regions.
- Pilot operational fixtures ran unchanged via their exported functions for all six viewport/theme contexts: **14/18 passed**. `smokeBlockchain` **6/6**, `smokeTeoriaScaleConstruction` **6/6**, `smokeBiasVariance` **2/6** desktop only. Bias mobile and narrow in both themes failed `page.waitForFunction: Timeout 10000ms exceeded` at the first decomposition expectation after fixed scroll positions 1200/2200/3200/4200/5200/6200 (smoke lines 9121–9133). Later slider/chart actions in those four invocations did not execute. Fixed-scroll fixture versus responsive scroll-scene behavior remains unresolved, not a proven engine defect and not a pass. Desktop did exercise decomposition, complexity, LOESS/KNN and double descent. Operational fixture output is in the session transcript.
- Post-edit `npm run check`, explicit `node --check tools/functional-sweep.mjs`, `npm run unit`, `npm run audit`, and `git diff --check` passed. Unit result **535/535**, zero failures/skips. No separate TypeScript/lint command is configured.

### Minimal fixes and retained failures

- `shared/public-footer.css`: scoped Loopy continuation `position: relative; z-index: 1100`. Before: the canvas intercepted continuation pointer events in all six cells; native disclosure still worked. After: continuation trial-click passes all six. Engines untouched.
- `shared/engineering-sandbox.css`: scoped bias–variance `#intro__hed { overflow-wrap: anywhere; }`. Before: document width 361px at 320px in both themes; range geometry identified the 52.8px “Separating” heading, not a chart. After: both narrow themes and the final full matrix pass viewport checks. No clipping/overflow-hiding workaround.
- Verification files: new `tools/functional-sweep.mjs`, export-only additions in `tools/smoke-bundle.mjs`, and this checkpoint. Existing dirty authored files were preserved. No route runtime was edited.
- First journal `/tmp/opencode/port171-functional-001.jsonl` retains 487 completed cells before the 1,000,000ms command timeout interrupted context cleanup (no complete row). It includes the two fixed regressions, the navigation failures, and an Atlas harness-only theme selector error subsequently corrected to `[data-home-theme-toggle]`. Do not count it as a completed sweep.
- `/tmp/opencode/port171-functional-resume.jsonl`: 18/18 remaining-route cells passed. `/tmp/opencode/port171-functional-fixed.jsonl`: 18/18 Atlas/Loopy/bias cells passed after fixes. These are supporting runs; the final 504-cell journal supersedes them for aggregate counts, without deleting their failures.
- One temporary diagnostic one-liner had a missing parenthesis and did not launch a browser; corrected checks are in the reusable sweep. No application failure inferred from that syntax error.

### Runnable aggregate assertion and next action

Run from the live repository:

```bash
node --input-type=module -e 'import fs from "node:fs"; import assert from "node:assert/strict"; const rows=fs.readFileSync("/tmp/opencode/port171-functional-final.jsonl","utf8").trim().split("\n").map(JSON.parse); const cells=rows.filter(r=>r.type==="cell"); assert.equal(cells.length,504); assert.equal(new Set(cells.map(r=>[r.slug,r.viewport,r.theme].join("/"))).size,504); assert.equal(cells.filter(r=>r.status==="passed").length,492); assert.equal(cells.filter(r=>r.status==="failed").length,12); assert(cells.every(r=>r.checks.includes("runtime-network-console"))); assert.deepEqual([...new Set(cells.filter(r=>r.status==="failed").map(r=>r.slug))].sort(),["covid-19","remember"]); assert.equal(rows.at(-1).type,"complete"); assert.equal(rows.at(-1).passed,492); console.log("504 unique completed cells; 492 passed; 12 retained navigation failures");'
```

Functional clearance remains incomplete: resolve the two generated-navigation contracts and diagnose bias–variance's four mobile/narrow operational timeouts using semantic scene targets before changing engines. Historical geometry/performance and unavailable human/device review are explicitly not promoted to passes and are nonblocking only as authorized by the user. Parent owns source-contract reconciliation separately; this browser checkpoint does not refresh or admit any source hashes.

## Browser-lane continuation — navigation and responsive fixtures

- Scope honored: no shared footer files, synth HTML, manifest, baseline, source-contract or engine changes in this continuation. Other agents concurrently edited footer/orientation and qualification files; therefore this is not immutable-source acceptance, and final orientation verification awaits the parent's completion signal.
- Root cause of 12 navigation failures: `remember` and `covid-19` already declared generated navigation before the current prose changes. Their visual dividers are not semantic headings and lack `data-story-chapter`; the shared shell therefore found zero sections. The current remember edit only adds a disclosure and covid edit only changes an opening paragraph; neither removed chapters. `shared/engineering-sandbox.js` now adapts existing `main > .divider` / `main .section.chapter` titles only for those two generated routes when no chapter seams exist. Native links/anchors and manifest assertions remain unchanged. Targeted journal `/tmp/opencode/port171-nav-adapter.jsonl`: **12/12 passed**.
- Root cause of four bias–variance timeouts: the operational fixture stopped at hard-coded desktop scrollY 6200. Responsive step 6 actually occurs near 10050 at 390px and 10352 at 320px. Sequential semantic step probes produced the expected decomposition and step-10 complexity labels in both sizes. No authored/engine defect was established; no HTML/CSS/runtime change was warranted. `smokeBiasVariance` now visits existing steps 1–6 semantically, retaining all decomposition, complexity, LOESS/KNN and double-descent assertions. All **6/6** bias operational cells now pass.
- Harness reliability changes: Atlas accepts an existing monitored page and does not reset the sweep viewport to 390px; default standalone smoke behavior remains. Sweep checks actual continuation target HTTP status, captures context-wide page/console errors including pilot pages, retains cleanup failures, and runs all three pilot operational fixtures within each of their six cells. A stronger monitor exposed harness theme seeding on opaque `about:blank`; `createThemeContext` now touches storage only for HTTP(S) documents, not an error allowlist. `/tmp/opencode/port171-nav-pilots-reliable.jsonl` retains the 36 failed context-console results from before that correction; all 18 pilot operation phases passed in that run.
- Full rerun `node tools/functional-sweep.mjs . /tmp/opencode/port171-functional-nav-fixed.jsonl`: **504 unique cells completed, 498 passed, six failed**. All **498 route continuation phases** and all **504 primary runtime/network phases** passed. All **18 pilot operation phases** passed. Context-wide errors make six blockchain cells fail, so this is not a clean pilot/site acceptance.
- Exact new hard failure, every blockchain viewport/theme: `Suggested Next Route unavailable. Error: Suggested Next Route unavailable`, at `mountRouteContinuation` in `shared/public-footer.js:421`. Existing `smokeBlockchain` visits `blockchain/distributed.html`, which declares `data-story-route="blockchain-distributed"` although that slug is absent from the manifest. Previously its page monitor did not reject console errors; the context-wide collector correctly retains this child-page failure. No console error was suppressed. Parent/footer owner must resolve the child-route continuation ownership; this lane avoided concurrently owned footer files.
- Current orientation phase passed 30/30 incidentally, but final close/click/synth verification is deliberately pending the parent's signal. This is not final orientation acceptance.
- Added `tools/tests/functional-sweep.test.mjs`: runnable native Node regression for bounded legacy divider title adaptation and preservation of existing chapter ownership. Check, explicit sweep syntax, audit and **538/538 unit tests passed** (count includes concurrent agents' additions).
- Strict command `node tools/smoke-bundle.mjs . --experience --route bias-variance` actually failed `Error: bias-variance desktop geometry baseline runtime bottom shifted by more than 1 CSS px`. Full historical polygons failure remains recorded above. No geometry comparison or baseline was removed or refreshed.
- Owned continuation files: `shared/engineering-sandbox.js`, `tools/smoke-bundle.mjs`, `tools/functional-sweep.mjs`, `tools/tests/functional-sweep.test.mjs`, and this checkpoint. No commits. Next: coordinate blockchain child continuation correction, then rerun its six cells and the final matrix/orientation after concurrent owners finish.

## Final signaled browser lane — completed functional scope

Parent explicitly signaled footer/synth/CI owners done; no concurrent source edits occurred during the final captures. Browser Chromium `148.0.7778.96`; HEAD `bbf680a92b25adbf5ca302268d7e7a94dec06cc8`. Dirty content is identified, not represented as a clean HEAD checkout.

- Final targeted journal `/tmp/opencode/final-targeted-clearance.jsonl`: **72/72 passed**, blockchain six cells plus all 11 orientation routes × six cells. Final full journal `/tmp/opencode/final-functional-504-clearance.jsonl`: **504/504 passed**, **498/498 route continuation checks**, **66/66 expanded orientation checks**, **18/18 pilot operational checks**, zero failed cells. Native aggregate assertions verified 504 unique keys, every cell passed, exact orientation/pilot counts, and source equality.
- Both successful journals record `source-before` and `source-after`: HEAD, dirty Git status, exact sorted file paths and SHA256 content hashes, aggregate SHA256 `5b12e74bf9d6e666295ce76fea9e4a44ff6686fe7c06a2b6f0cf9f0d936a01ad`, **6005 files**, identical before/after. Reuses stdlib-based `sourceIdentity`, excludes `.git`/`node_modules`, hashes site assets and current verification code rather than timestamps. Raw journals are outside the tree. Added native regression verifies timestamp-only changes preserve identity while dirty bytes change it.
- Sweep lifecycle now initializes server/browser inside try/finally, records aborted runs, and closes server/journal even when browser launch or browser cleanup fails. Orientation tests cover generated five and six static `[data-route-orientation]` synth disclosures: pointer and Enter/Space toggle, paragraph click, Escape with summary focus restoration, focusout, outside pointerdown, closed overlay/continuation hit-testing. Coming Out wraps but still invokes the real narrative window click handler and asserts zero invocations from internal disclosure actions; no narrative engine is replaced.
- Final verification exposed and fixed two real authored interaction regressions. `shared/public-footer.js` synchronous focusout dismissal closed details during a nonfocusable paragraph pointerdown, reproducibly crashing Chromium in a minimal Trust mouse probe. Track internal pointer gesture until pointerup/cancel so internal clicks do not collapse content under the pointer; ordinary focusout, Escape and outside-pointer dismissal remain tested. `shared/public-footer.css` adds synth-only continuation bottom clearance (`6rem` plus safe area): the wrapped closed summary intercepted continuation at 320px on all six synth routes in both themes. No overlay hiding, pointer disabling or engine changes.
- Failed evidence retained: `/tmp/opencode/final-orientation-targeted.jsonl` aborted after 29 recorded cells and a 400000ms timeout following renderer crashes; not complete. `/tmp/opencode/final-orientation-targeted-fixed.jsonl` and `/tmp/opencode/final-functional-504.jsonl` retain narrow synth occlusion failures before clearance. The latter completed **492/504**, exact failure was summary interception of `[data-route-continuation-link]` on all six synth narrow light/dark cells. No retry result replaced those journals.
- Dedicated unchanged `node tools/rigid-body-browser.mjs .` passed again: **18 repeated + four fallback = 22 contexts**, six viewport/theme cells. Full raw collision samples are in the tool transcript. No paired timing acceptance implied.
- Actual final `node tools/smoke-bundle.mjs .` failed `Error: polygons desktop geometry baseline runtime bottom shifted by more than 1 CSS px`; actual `node tools/smoke-bundle.mjs . --experience --route bias-variance` failed `Error: bias-variance desktop geometry baseline runtime bottom shifted by more than 1 CSS px`. Historical gates remain intact, unrefreshed, and nonblocking only under the user's stated release scope.
- `npm run check`, explicit sweep syntax, audit, diff whitespace, and **540/540 unit tests passed**, zero failed/skipped/cancelled. First unit run caught an obsolete expected listener inventory; updated it to include the newly tested pointerup/pointercancel listeners, then reran successfully. This test-only correction occurred after browser capture: content digest then `2d4c4fefa0822870411efcceb626931b6086993c530bde9af270f0a6b8f56783`; native comparison confirmed the only changed file was `tools/tests/route-html-sync.test.mjs`. This appended checkpoint additionally changes documentation, not captured runtime bytes. Do not conflate either post-capture digest with the captured digest.
- Final-signal owned edits: `tools/functional-sweep.mjs`, `tools/tests/functional-sweep.test.mjs`, `shared/public-footer.js`, `shared/public-footer.css`, `tools/tests/route-html-sync.test.mjs`, and this checkpoint. Parent's child continuation fix is preserved and blockchain six-cell console checks now pass. No comments, commits, pushes, baseline/admission hash updates or deployment.

No known unresolved hard functional failure remains within the executed scope. Coverage is all 83 routes plus Atlas readiness/runtime-surface, shell/native contracts, continuation/network/console and theme checks at three viewports in both themes; detailed operations for the three pilots and collision, and the 11 orientations. It is not exhaustive every-engine/action/audio/device certification or a completed strict59/paired-performance qualification. Parent may proceed under the authorized functional release scope, retaining historical failures and these limits honestly.

## Release canonicalization — 2026-09-13

Parent explicitly approved sync-generated formatting in 51 parity JSON files (parsed values unchanged), formatting-only canonicalization of `public-private-keys/index.html` (script elements and order unchanged), and moving the collision `color-scheme` meta after canonical/og metadata and before the theme script. These changes belong only to `/tmp/opencode/complete-route-release`; the original dirty source and three excluded evidence files remain untouched. Extra terminal blank lines in checkpoints 03/04/07 are removed without changing historical content.

The retained `/tmp/opencode/final-functional-504-clearance.jsonl` still identifies its original 6005-file capture with digest `5b12e74bf9d6e666295ce76fea9e4a44ff6686fe7c06a2b6f0cf9f0d936a01ad`; it is not a capture of these canonical bytes. Of the 3714 runtime files (manifest route directories, shared assets, Atlas and both manifests), 3712 remain byte-identical and exactly the two approved HTML files differ. Canonical runtime digest is `facedf726b5516646c0d07d00e5047f6896ad580cf4787ef60ea17760ec83dd4`. The two-route 12-cell functional sweep and dedicated 22-context collision acceptance must be recorded separately; historical geometry/performance limits remain unchanged. No commit, push or deployment is authorized in this verification step.

Canonical verification passed: `npm test` completed sync/audit/syntax and **540/540** unit tests, with no new tracked diff after staging; staged whitespace check passed. The sweep accepts positional slugs, not `--route` (a read-only preflight rejected that spelling before browser launch). Actual command `node tools/functional-sweep.mjs . /tmp/opencode/complete-route-release-canonical-12.jsonl public-private-keys rigid-body-collisions` passed **12/12** unique cells with identical before/after source identity. Then `node tools/rigid-body-browser.mjs` passed **18 repeated plus four fallback contexts**, sequentially in Chromium `148.0.7778.96`; full collision JSON is retained in the session tool output. No other browser lane was started. This results paragraph is a post-capture documentation-only addition, not part of the journal's full-source digest. The old full504 journal is preserved, not rewritten or relabeled as a canonical full rerun.
