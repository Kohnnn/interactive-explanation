# 06 Opaque Route slices

Status: ready-for-agent
Spec: [Master specification](../spec.md)
Blocked by: [05 Runtime-hook slices](05-runtime-hook-slices.md)
Execution: not started
Wave: W4 — exactly 27 Routes

## Exact membership by canonical family

- ncase (13): `trust`, `polygons`, `ballot`, `crowds`, `loopy`, `neurons`, `remember`, `anxiety`, `wbwwb`, `coming-out-simulator-2014`, `covid-19`, `simulating`, `sim`
- ev-essay (1): `markov-chains`
- engineering-longform (5): `bicycle`, `airfoil`, `internal-combustion-engine`, `mechanical-watch`, `naval-architecture`
- systems-essay (1): `rigid-body-collisions`
- runtime (7): `formula-1-racing`, `interactive-mechanical-watch`, `reading-qr-codes-without-a-computer`, `chrome-music-lab-song-maker`, `musicmap`, `stargazing-dashboard`, `watch-mesh-explorer`

Membership and per-Route ownership come from the [snapshot](../research/35-route-rollout.json), not smoke families. Fixed-runtime entries remain fixed; shell-only entries do not grant permission to edit intrinsic engines.
This package supersedes retired legacy NCase 24/25, compiled 26, binary 27 and stargazing 28. Retirement does not claim they were implemented. No pilot or Atlas implementation is repeated.

## Scope and next-session boundary

After W3 gates, implement exactly one Route per slice and rollback unit. First slice: rigid-body-collisions, beginning with package 01 favicon disposition and authored boundary verification.
Before every adaptation, establish declared primary/runtime readiness, native navigation and child ownership; record what can be changed without runtime interference.
Preserve opaque engines, compiled/vendor/archive files, script dependencies, native gestures, backing dimensions, numerical results, local save/export data and pointer-coordinate mapping.
Use bounded authored orientation, token-scoped chrome, accessible fallback and named continuation only; no universal wrappers, forced essay rails or intrinsic palette changes.
Investigate markov-chains child cancellation separately; musicmap primary and anxiety continuation require readiness evidence, not a parent snapshot verdict. Generic main absence on loopy is not primary-surface failure.
Address confirmed authored overflow through usable reflow or explicitly justified bounded two-dimensional scrolling, never clipping masks, global overflow hiding or renderer shrinking.

## Observable acceptance

- Every Route retains launch/deep-link/subpath behavior, intended native control operation, local data/theme persistence, and manifest-owned continuation without unsolicited focus or new Progress.
- Both themes apply to owned chrome while excluded intrinsic output retains its meaning. Loading/ready/error/recovery states and child interactions have separate evidence, not inherited parent passes.
- Check keyboard/pointer/touch, reduced motion, focus/zoom, contrast and 24px AA minimum/44px target on authored controls at all three viewports; report inaccessible intrinsic boundaries honestly.
- State and geometry remain stable through theme/reflow; intended authored changes have separate agent-reviewed successor baselines preserving history.
- Zero new unexpected runtime/network errors or unexplained geometry. Existing signals require disposition, never wildcard suppression. Resource deltas are declared and paired same-environment three-context budgets hold.
- Attribution/license obligations and truthful adapted/new provenance survive even if legacy public-surface policy must be narrowly reconciled.

## Commands and gate

Run from parent `interactive-note/` root for the single current slug:
`npm --prefix interactive-explanation run check`
`npm --prefix interactive-explanation run unit`
`node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route <slug>`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route <slug> --experience`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation`
Use strict experience once the current Route is migrated; focused readiness/child probes precede it. Full regression is mandatory for shared edits and W4 closure, not substituted by one opaque sample.

## Checkpoint and stops

Append each Route checkpoint here: completed/remaining exact slugs, supported seam, owned hunks/preimage, source/environment, commands/evidence, child/device gaps, baseline decision and next action.
Resume only after checking the last gate and source drift. Reverse only current owned hunks; preserve unrelated dirty work, user data and historical evidence. Stop for ownership overlap or a required opaque edit.
Material out-of-scope engine/child adaptations require a human decision; matching authored work receives agent review. Blocked real-device, assistive-technology or child modalities remain final-release blockers.
All 27 Routes and W4 gates must pass before package 07. No automatic install, commit, deployment, or release.

## Flattened repository port — 2026-09-13

Imported append-only from the frozen parent integration at `6111af8d7a53a1e716f3b7b784072c3c55c3d45a` plus authored edits onto live `bbf680a92b25adbf5ca302268d7e7a94dec06cc8`. Earlier header status is historical: 21 route-local adaptations and five shared orientation disclosures are implemented; W4 acceptance remains open. Parent `interactive-explanation/` paths map to this repository root; parent Git commands and reported checks remain historical parent-context evidence. Use root npm scripts and `node tools/smoke-bundle.mjs .` here. The original collision lab already exists in live ancestry and was not ported or overwritten. The live Research 53 Sim parity correction is preserved. No browser or acceptance result is implied by this port.

## Source-pass checkpoint — 2026-09-13

User authorized this bounded source pass despite W0 timing and unavailable modalities, with no browsers. This does not waive qualification or close W3/W4. Worktree: `/tmp/opencode/complete-route-integration`; HEAD/preimage: `6111af8d7a53a1e716f3b7b784072c3c55c3d45a`; Linux, Node v24.20.0. Initial dirty work belonged to other lanes (54 tracked files reported, later 57); none of the 26 reviewed route indexes/parity records overlapped. No shared, manifest, baseline, engine, minified, comment, commit or browser changes in this lane.

`rigid-body-collisions` is reserved, not inspected or edited here, including its parity. User reports original1D collision lab in `/tmp/opencode/ci-timing-qualification` at `63a8bddc`; deployment agent investigates porting separately. That report is not verified or accepted by this checkpoint.

Each row below is a separate source-review/rollback unit. Source reviewed is `interactive-explanation/<slug>/index.html` at its opening/owned seam plus matching `docs/<slug>/parity.json`; existing source citations are retained, not claimed as newly reviewed engines. Stargazing additionally received targeted weather/state searches in `js/state.js`, `js/hud.js`, `js/scene.js`. Musicmap's serialized body was truncated; only head/opening mounts were reviewed. Canonical family/ownership of all 83 rows and exact W4=27 were checked against the rollout snapshot, not inferred smoke families.

| Exact slug | Disposition and owned hunk | Source-reviewed applicability / remaining boundary |
| --- | --- | --- |
| trust | Skip; no owned ready-state paragraph outside slideshow | Preload/error, chapters, strategy/sandbox, audio; native timing owns orientation |
| polygons | Adapt first chapter paragraph: neighborhood question, drag task, segregation reason, housing-model limit | Independent boards, drag/replay, child readiness and frame focus; iframe sizes unchanged |
| ballot | Adapt opening paragraph: counting rule, drag/compare, vote splitting, spatial-model limit | Child elections/methods and frame focus; geometry signal unresolved |
| crowds | Skip; staged words and scratch canvas are runtime-owned | Drawing, puzzles, sandbox, chapters, modal/audio; no safe hero |
| loopy | Skip; sidebar/canvas are generated mounts | Edit/play, drawing, URL models/save/export; #sidebar is primary, generic main is not its readiness test |
| neurons | Adapt existing post-iframe description: learning question, animation/sandbox comparison, clinical limit | Iframe-first pacing, captions/replay/sandbox; child acceptance separate |
| remember | Skip; positioned panel words and deferred frames | Scroll/chapter pacing, practices/video, child readiness; existing overflow unresolved |
| anxiety | Skip; available prose is gated content-note/about dialog content | Dialogue/choices, options/replay/audio; continuation must be tested at ready state |
| wbwwb | Skip; stage owns narrative, loading copy disappears | Camera capture, pause/end/replay, audio/WebGL failure; credits untouched |
| coming-out-simulator-2014 | Skip; parent only offers loading/end text | Branch choices, audio/replay; preserve conversation pacing and unresolved narrow geometry |
| covid-19 | Adapt first paragraph: contacts/immunity question, single-setting curves, historical May 2020 limit | Child start/replay/settings, footnotes/chapters; no current forecast/medical guidance |
| simulating | Adapt existing heading/paragraph: local rules, editor/article choice, repeated runs, toy limit | Local handoffs; no parent playback/reset/score, nested tasks require separate checks |
| sim | Skip; viewport-bound grid/editor has no independent orientation | Draw/edit/play/pause/step/reset, speed/save/load; existing typography/geometry adapter untouched |
| markov-chains | Adapt hero title/summary/task: transitions, self-transition comparison, probabilistic and conditional-memory limits | Parent matrix/speed bindings retained; playground readiness/cancellation unresolved |
| bicycle | Adapt hero title/summary/task: forces, rider/camera action, velocity comparison, design/safety limit | Binary renderer, sliders, scientific color, chapters unchanged |
| airfoil | Adapt hero title/summary/task: lift, angle/pressure comparison, cross-section limit | Flow/binary scenes and opening prompt untouched; markers not a force measurement |
| internal-combustion-engine | Adapt hero title/summary/task: piston/rotation, camera/pause, mechanism limit | Camera angle distinguished from cycle time; shared animation behavior retained |
| mechanical-watch | Adapt hero title/summary/task: spring/timing, cutaway, transmission/regulation, accuracy/service limit | Shared binary watch, layers/pause and chapters unchanged |
| naval-architecture | Adapt hero title/summary/task: afloat/upright, slider/forces, seaworthiness limit | Floating distinguished from restoring torque; water/renderer roots unchanged |
| formula-1-racing | Adapt hero title/summary/task: corner/straight tradeoff, single-control comparison, non-telemetry limit | Procedural canvases, linked lap state/readouts and controls unchanged |
| interactive-mechanical-watch | Adapt hero title/summary/task: transmission/regulation, cutaway, accuracy/service limit | Reference link retained; shared watch and separate teardown/inspector require independent readiness |
| reading-qr-codes-without-a-computer | Skip; compiled #app owns ready lesson | Module failure/readiness, mask/bit/payload controls, narrow layout; no bundle edits |
| chrome-music-lab-song-maker | Adapt owned heading/paragraph: move note, paint/play/compare, pitch/time, audio/mic/save limits | Body-mounted sequencer, settings, audio permission, disabled remote save retained; header ready-state visibility unverified |
| musicmap | Skip; no independently owned orientation established in serialized bundle shell | Primary visibility, zoom/search/genre/media panes, click-only embeds; readiness signal unresolved |
| stargazing-dashboard | Adapt hero title/summary/task: horizon, time/location/altitude, rotation, weather-scenario limit | WebGL/catalog fallback, HUD, dialogs and observing state unchanged; no live forecast implied |
| watch-mesh-explorer | Adapt one existing legend sentence with inspection task/service limit; retain existing question/mechanism/result | Runtime-bound lesson/mode text untouched; binary readiness, filter/selection, playback/reset separate |

Authored source-pass adaptations (16): `polygons`, `ballot`, `neurons`, `covid-19`, `simulating`, `markov-chains`, `bicycle`, `airfoil`, `internal-combustion-engine`, `mechanical-watch`, `naval-architecture`, `formula-1-racing`, `interactive-mechanical-watch`, `chrome-music-lab-song-maker`, `stargazing-dashboard`, `watch-mesh-explorer`.

Unsafe-seam skips, unchanged indexes (10): `trust`, `crowds`, `loopy`, `remember`, `anxiety`, `wbwwb`, `coming-out-simulator-2014`, `sim`, `reading-qr-codes-without-a-computer`, `musicmap`. Matching parity records explicitly record each skip, not migration completion. Reserved remaining route: `rigid-body-collisions`. All 27 remain unaccepted; no unreviewed member remains among the other 26 at the bounded source seam.

### Preimages, review and checks

Owned preimages are retrievable with `git show 6111af8d7a53a1e716f3b7b784072c3c55c3d45a:interactive-explanation/<slug>/index.html` and matching parity path. Reverse only the listed text hunks and the added `package06-w4-source-review` parity object, never whole-tree reset or unrelated changes. For the 52 paths (26 routes in snapshot order, each index then parity), SHA-256 over path + NUL + bytes + NUL: preimage `67950ebdbb6d440489fa05becff6b2171947ccaafbd21ee613a41348bd52719c`; postimage `c8a40b81f738a5207f4a35b68cd90564f23786215fe948a675ee1ed1e6d9e734`.

Static commands executed from the worktree root:

- `npm --prefix interactive-explanation run check`: passed configured syntax checks; no separate lint/typecheck configured.
- `npm --prefix interactive-explanation run unit`: 373 passed, 0 failed. Unit fixtures are not browser acceptance or production manifest synchronization.
- `node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`: passed.
- Inline Node/assert guard: 83 unique canonical rows matched snapshot family/ownership, W4=27; all 26 non-reserved parity records parse and contain exactly one source-review object; 16 changed indexes, 10 byte-identical indexes. Compared HTML tag sequences and complete inline script blocks with HEAD: unchanged. First attempt caught deleted ballot emphasis tags; restored the existing `<em>` structure and reran successfully. This check does not measure rendered layout.
- Scoped `git diff --check`: passed for all 52 paths.
- Direct agent diff review, not independent subagent/human review: Standards — no new logic, dependencies, comments, selectors or wrappers. Spec — existing authored text seams only; runtime/attribution markup retained. Partial implementation is explicit for the ten unsafe seams and reserved route; no whole-W4 completion claimed.

Expected resource delta: no added/removed requests or payload classes; only HTML text and non-runtime parity bytes change. Copy may change authored line wrapping and document offsets. No intrinsic dimensions, CSS, scripts, native control labels/bindings, state, source notices or links were edited. Identical markup is not proof of identical viewport allocation or pointer mapping.

### Qualification and next action

No browser, smoke, strict experience, baseline recording or performance samples were run. No baseline successor approved. For every row, themes at 1400×1000, 390×844 and 320×844; system/saved/storage-denied themes; keyboard/pointer/touch; focus/zoom/contrast/targets; reduced motion; applicable loading/ready/error/recovery; continuation; geometry and three-context paired performance remain unqualified. Child/audio/WebGL/real-device/assistive-technology limits cannot inherit parent or static passes. No learner-outcome or clinical claim is accepted.

Next: parent reviews source drift and each owned hunk, obtains route-specific ready/child evidence and rendered comparisons without changing engines, then schedules serial focused/strict checks and full regression when authorized. Keep W0 timing, prior overflow, musicmap primary, anxiety continuation and Markov child-cancellation obligations open. Coordinate rigid-body work only with the deployment lane; package 07/release remains blocked.

## Follow-up research and implementation — 2026-09-13

User explicitly requested useful orientation beyond an opening hero, allowing existing help text or nonmodal post-runtime details. This supersedes five earlier opening-only skips; the preceding checkpoint remains historical. Same integration worktree and scope; no browsers, shared edits, engine/compiled edits, commits or rigid-body work. Research used local primary source directly; no background-agent tool was available, so findings are recorded here rather than attributed to another reviewer.

### Researched seams and decisions

- Shared footer source: `interactive-explanation/shared/public-footer.js:264-328` skips generation when `.public-footer` already exists, otherwise creates a hidden/inert reference footer unless explicitly opted in. `:332-383` inserts manifest-owned continuation before it. Consequently a static fake `.public-footer` would suppress provenance generation; a `.route-continuation` reuse risks confusing continuation checks. Neither class is borrowed. `shared/public-footer.css:4-14` supplies existing editorial color/font tokens used by the new disclosures. No supported orientation-data attribute currently exists; recommendations below require parent-owned shared work rather than pretending one does.
- `remember/css/comic.css:10-22` permits vertical document scrolling; `remember/index.html:1839` closes main before hidden labels and script handoff. `remember/js/comic.js:12-18,346-381` selects custom panels/words/sims and tests individual iframe bounds, not a final document-height progress ratio. A normal-flow disclosure after main is outside those intrinsic selectors and does not precede any comic panel. Implemented there.
- QR `assets/index-s1eyThQf.css:1` uses normal flow with a max-width body, not a full-viewport app stage. `index.html:47-81` observes only #app content for readiness; `a11y-labels.js:7-28` finds the app table and text inputs. A details sibling introduces neither app children nor inputs. Implemented after #app, before the existing accessibility script, keeping script order unchanged.
- `crowds/index.html` contains static `bonus#contagions` help, separate from staged `<words>` content. `js/slideshow/Modal.js:3-9,46-75` copies that help into the existing large Bonus Boxes modal; `css/index.css:275-295` fixes modal geometry and enables content scrolling. Replaced only its introductory prose. No modal, trigger or pacing state added.
- `anxiety/index.html:102-106` has an existing content-note paragraph. `styles/game.css:267-299` fixes its box at 300 by 440 plus existing padding/margins. Replaced that paragraph with shorter question/action/consequence/clinical-limit text; preserved content warnings, support link and the OK gate. Actual line wrapping remains a browser obligation, not inferred from character count.
- Musicmap's serialized body initially exceeded Read/Grep limits. Targeted read-only `rg -o` extraction of headings/paragraphs and CSS rules found a static Navigate instruction: "Browse genres alphabetically or search any genre name on the Carta. Then click on the arrow button to navigate towards your selection on the Carta." Replaced only this paragraph with similarly sized question/choose-arrow-inspect/influence/genre-boundary copy. Extracted `.small-side-pane-paragraph` and fixed pane rules in `main.css`; no full bundle review claimed. The earlier lack-of-seam conclusion is withdrawn for Musicmap.

### Implemented exact slugs (5)

| Slug | Minimal authored change | Guard / remaining qualification |
| --- | --- | --- |
| `remember` | Collapsed native `details#route-orientation` after main: spaced recall question, try/reveal/revisit task, retrieval reason and no permanent-memory guarantee | No panel, label, iframe, script or preceding geometry markup changes; native disclosure focus/touch/zoom and deferred child behavior still unmeasured |
| `reading-qr-codes-without-a-computer` | Collapsed native after-#app details: structure/mask/bit/payload task and damaged-code/general-decoder limit | Original app subtree and scripts unchanged; compiled layout, disclosure state and continuation still need runtime evidence |
| `crowds` | Existing Bonus Boxes contagion-help paragraph: same-network simple/complex comparison and reinforcement/model limit | Original tags/scripts unchanged; existing modal accessibility/scroll/readiness not certified |
| `anxiety` | Existing content-note paragraph: protection/fear question, choose warnings, observe human response, not diagnosis/treatment | Original tags/scripts/gate retained; therapeutic-benefit implication removed; fixed-box wrapping and continuation readiness remain open |
| `musicmap` | Existing Navigate help paragraph: genre relationships, choose/arrow/inspect, influences not strict boundaries | Original serialized tags/scripts and graph unchanged; pane visibility, wrapping, keyboard and primary readiness remain open |

The two details are closed by default, use native summary behavior, retain the visible disclosure marker and default focus outline, target 44px summary height, and use existing editorial tokens with element-local styles only. They are not runtime wrappers, modals, acceptance badges or extra continuation links. They add trailing authored height; opening a disclosure must not be described as zero document-geometry change. No new JS, resources, persistent state or runtime controls.

### Exact remaining entrypoint limitations and parent action (5)

| Slug | Primary-source blocker, not merely absent hero | Proposed shared orientation content / placement obligation |
| --- | --- | --- |
| `trust` | `css/slides.css:7-48`: full-height body with overflow hidden, percentage-height main and absolute native footer; after-main content shares native footer space or is clipped | "When does cooperation pay? Compare choices across repeated rounds; future encounters change incentives. This payoff model is not a prediction of real relationships." Add shared nonmodal orientation/continuation placement outside #main and #footer; keep their original containing block and height |
| `loopy` | `css/loopy.css:1-21,44-58`: overflow-hidden body, viewport canvas and absolute transport; sidebar/help generated by engine | "Can a feedback loop amplify a change? Draw a loop, choose positive or negative links, then play and perturb a node. Signs describe qualitative influence, not measured quantities." Shared slot must not enter #canvasses/#sidebar, steal drawing shortcuts or reset URL/save state |
| `sim` | `styles/model.css:1-9,22-27,53-63` plus entrypoint 100vh sizing: body hidden overflow, absolute editor; prose generated in runtime | "What patterns can local rules create? Change one rule, step the model and compare repeated runs. The rules define this toy world, not a forecast of a real population." Shared slot needs reachable scrolling without changing grid dimensions, editor textarea autosizing or ui/resize timing |
| `wbwwb` | `css/game.css:1-48`: relative full-height body is containing block for absolutely centered stage/overlays; a normal-flow sibling starts at top, not after stage; enlarging body can recenter game | "How can a headline feed a cycle of fear? Photograph a moment and watch the crowd respond to its broadcast. This scripted satire is not an empirical model of media effects." Shared below-viewport placement must preserve original body/stage centering, cursor behavior and credits link |
| `coming-out-simulator-2014` | `css/game.css:1-28`: overflow-hidden body and absolute 360 by 640 stage; entrypoint has only loading/end text, opening choice prose is script-owned | "What changes when you choose a different reply? Follow the conversation and notice how others respond. This authored personal story is not a guide to predicting a family's reaction or a requirement to come out." Shared placement must retain native choices/keyboard and not turn replay/end copy into forced preamble |

Parent recommendation: extend the existing footer/continuation mounting seam with an explicit optional route-keyed orientation source and placement policy for these five, leaving attribution generation and manifest continuation intact. Use native nonmodal disclosure rather than a forced hero. A generic body append is insufficient: qualify each route's clipping, containing block, global input listeners, scroll reachability, theme and runtime readiness. Do not enable or fabricate footer provenance merely to expose orientation. This is a concrete shared-lane proposal, not an existing API or authorization to change the engines. No shared edits performed here.

### Static results and rollback

- Configured `npm --prefix interactive-explanation run check`: passed.
- `npm --prefix interactive-explanation run unit` (through RTK test output): passed, zero failures.
- Public-surface audit: passed.
- Inline Node/assert guard across the exact ten: five changed indexes, five byte-identical to `6111af8d7a53a1e716f3b7b784072c3c55c3d45a`; all original HTML tag sequences match after subtracting only the two `details#route-orientation` blocks; all complete inline script blocks unchanged; both disclosures occur after main and lack open; all ten parity JSON files parse. Scoped diff whitespace check passed.
- Matching parity source-review objects updated for all ten, preserving older module histories. For the five remaining, parity includes the exact shared recommendation and source constraints, not a repeated absent-hero excuse.

Rollback only this follow-up: remove the two identified details blocks, restore the three exact help paragraphs from the commit preimage, and reverse the ten parity source-review object changes plus this checkpoint append/status line. The preceding 16 adaptations and other lanes remain untouched. Existing checkpoint postimage hash describes the previous source pass, not this follow-up.

Current W4 authored source work: 21 adaptations total, five parent shared-placement recommendations (`trust`, `loopy`, `sim`, `wbwwb`, `coming-out-simulator-2014`), `rigid-body-collisions` excluded/reserved and unchanged here. All rendered geometry, browser/device/child/accessibility/performance and release acceptance obligations remain open. Next parent action is the five-route shared placement decision plus focused qualification of these five implemented seams when browsers are authorized.

## Shared orientation integration checkpoint — 2026-09-13

Implemented exactly `trust`, `loopy`, `sim`, `wbwwb`, `coming-out-simulator-2014` in `/tmp/opencode/complete-route-integration`. Owned hunks: `shared/public-footer.js` adds `mountRouteOrientation` and one call at the existing continuation insertion seam; `shared/public-footer.css` adds only `.route-continuation > .route-orientation` rules; `tools/tests/route-html-sync.test.mjs` adds one test; each matching `docs/<slug>/parity.json` adds one `package06-shared-orientation` record; this checkpoint/status line. All paths except this checkpoint are relative to `interactive-explanation/`. Existing dirty CSS redesign, prior test, route adaptations and parity history are retained. No entrypoints, engine files, manifests, baseline, dependency or commit changes.

The five concise question/action/reason/limitation strings are local to the footer helper and keyed by existing `data-story-route`; no new shared registry, config contract or request. Native collapsed details mounts inside the existing continuation section before reference-footer, on both normal and manifest-fallback insertion. The details itself is fixed bottom-right, bounded to the viewport and internally scrollable, not new document flow. Existing body/runtime dimensions, containing blocks, continuation placement/link generation and hidden/inert provenance behavior are untouched. This intentionally uses an in-viewport disclosure rather than the earlier proposed below-viewport placement, which would require scrolling/containing-block changes. Closed/open disclosure still occupies visual space and can overlap runtime controls: source-only work does not certify that placement.

Only disclosure keydown/keyup bubbling is stopped, retaining native summary defaults and focus behavior without document listeners or persistent state. This is not proof against capture-phase handlers, global pointer handling or runtime modal conflicts. No runtime state reads/writes, resize events, autofocus, new progress or forced preamble.

Checks actually run: `npm --prefix interactive-explanation run check` passed; `npm --prefix interactive-explanation run unit` passed 374/374; `node interactive-explanation/tools/check-public-surface.mjs interactive-explanation` passed; scoped `git diff --check` passed. No separate lint/typecheck configured. The added VM/assert test checks five distinct copies, unrelated/prototype-key exclusions, collapsed native markup, local key isolation, continuation insertion seam and fixed/scrollable CSS source. It is not a rendered geometry or full boot/fallback integration test.

Rollback only this integration: remove the helper/call, four orientation CSS rules, the named orientation test, five new parity records and this checkpoint/status change. Do not restore whole shared files from HEAD: their pre-existing redesign and other lanes are dirty. Historical source-pass hashes above do not describe this integration.

Remaining for parent: browser focused/strict checks for all five and shared full regression; closed/open placement, native control/credits occlusion, summary reachability, scrolling, native keyboard/pointer/touch, modal interactions, theme/focus/zoom/contrast, loading/ready/error, continuation and runtime state/dimensions at 1400×1000, 390×844 and 320×844. Three-context paired performance, real-device/assistive-technology and all prior W0/W4 blockers remain open. No browser run, acceptance, baseline successor or release claim. Source adaptation count is now 26 (21 route-local plus these five shared); reserved rigid-body work is not included.
