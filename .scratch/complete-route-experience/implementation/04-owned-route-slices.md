# 04 Owned Route slices

Status: ready-for-agent
Spec: [Master specification](../spec.md)
Blocked by: [03 Intent pilots](03-intent-pilots.md)
Execution: not started
Wave: W2 — exactly 31 Routes

## Exact membership by canonical family

- mlu-pilot (9): `decision-tree`, `random-forest`, `linear-regression`, `logistic-regression`, `precision-recall`, `roc-auc`, `train-test-validation`, `double-descent`, `double-descent2`
- ev-essay (8): `conditional-probability`, `principal-component-analysis`, `exponentiation`, `pi`, `sine-and-cosine`, `eigenvectors-and-eigenvalues`, `image-kernels`, `ordinary-least-squares-regression`
- engineering-longform (10): `alpha-compositing`, `color-spaces`, `sound`, `cameras-and-lenses`, `lights-and-shadows`, `tesseract`, `gears`, `gps`, `earth-and-sun`, `curves-and-surfaces`
- local-hub (2): `music-interactive-hub`, `primary-interactive-hub`
- systems-essay (1): `hysteresis-slack`
- anders-lab (1): `blockchain-101-combined-flow`

All are shell-only in the [snapshot](../research/35-route-rollout.json); that permits authored changes, not intrinsic descendant recoloring. No pilot or Atlas implementation is repeated here.

## Scope and next-session boundary

After all pilots pass, select at most five Routes of one canonical family per slice; record exact membership before editing. First slice: decision-tree and random-forest, then expand only after their gate.
Adapt owned prose, hierarchy, controls and root-scoped styles to the selected system; retain each Route intent instead of imposing an essay on hubs or the combined lab flow.
Preserve native train-test-validation navigation, generated chapter/deep-link behavior elsewhere, explicit continuation graph, Music Guided Path Progress and unnumbered Primary semantics.
Use package 01 signal dispositions for narrow exponentiation, regression/precision/train-test and combined-flow geometry; reproduce at readiness rather than hide overflow.
Keep intrinsic scientific colors, math output, media geometry, numerical behavior, runtime placement and supported pointer mapping. Do not edit compiled/vendor/archive assets.

## Observable acceptance

- Each selected Route passes its applicable master component/state matrix in both themes and three viewports with keyboard/pointer/touch, saved/system preference, reduced motion and zoom.
- Reading order, chapter entry, controls/feedback, next link and local subpath assets remain usable; theme changes do not reset runtime or saved data.
- Authored text/control contrast and 24px AA minimum/44px design target hold; no masked clipping or lost focus. Intentional reflow has a reviewed successor baseline preserving history.
- Zero new unexpected runtime/network failures and zero unexplained geometry; resource deltas declared and paired three-context DCL/load/transfer measurements satisfy inherited relative ceilings.
- Claims/diagrams remain accurate, especially non-monotonic complexity examples; provenance distinguishes retained/adapted/new expression and preserves required licenses despite audit conflicts.

## Commands and gate

Run from parent `interactive-note/` root; repeat the explicit Route flag for every member of the current slice:
`npm --prefix interactive-explanation run check`
`npm --prefix interactive-explanation run unit`
`node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route <slug> --experience`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation`
Focused strict gates precede each next slice; full smoke is mandatory for shared edits and W2 closure. Smoke group names cannot substitute for this exact canonical-family membership.

## Checkpoint and resume

Append current family/slice, completed and remaining exact slugs, owned hunks/preimages, source/environment, commands/evidence, blocked states, baseline approval and next action here after every session.
Do not mark 31 Routes complete after a representative pass. Resume by checking drift and the last slice gate; no one-session atomic-wave assumption.
Stop on state/URL/data loss, inaccurate claims, licensing uncertainty, budget/hard regression, ownership overlap or an opaque-edit requirement. Reverse only owned hunks; preserve unrelated dirty work and historical evidence.
Routine matching work receives agent review; material out-of-scope departures require a human decision. Missing child/device/assistive-technology coverage remains a final-qualification obligation, never a blanket pass.
Package 05 begins after all 31 Routes and W2 gates pass. No automatic installation, commits, deployment, or release.

## Flattened repository port — 2026-09-13

Imported append-only from the frozen parent integration at `6111af8d7a53a1e716f3b7b784072c3c55c3d45a` plus authored edits onto live `bbf680a92b25adbf5ca302268d7e7a94dec06cc8`. Earlier header status is historical: all 31 authored route/parity pairs are implemented; W2 acceptance remains open. Parent `interactive-explanation/` paths map to this repository root; parent Git commands and reported checks remain historical parent-context evidence. Use root npm scripts and `node tools/smoke-bundle.mjs .` here. No browser or acceptance result is implied by this port.

## Bounded authored checkpoint — 2026-09-13

- User authorizes isolated package04 source slices despite unresolved W0 timing/review and separately owned package03 pilots. This authorization does not establish acceptance or waive the first-slice expansion gate.
- Source: `/tmp/opencode/complete-route-integration`, HEAD `6111af8d7a53a1e716f3b7b784072c3c55c3d45a`; existing unrelated dirty shared/Atlas/package05/evidence work and dependency symlink preserved. All four owned route/parity files and this checkpoint were clean at entry; their exact preimages are the corresponding HEAD blobs. Rollback only these owned hunks, never the whole working tree.
- Declared/implemented slice 1, canonical mlu-pilot: `decision-tree`, `random-forest`. Owned files: `interactive-explanation/decision-tree/index.html`, `interactive-explanation/random-forest/index.html`, their matching `interactive-explanation/docs/<slug>/parity.json`, and this existing checkpoint. No other files edited by this session.
- Decision-tree adapts the existing hero with a class-separation question, pure/mixed entropy task, weighted child-entropy observation/reason and unseen-data limit. Random-forest adapts the hero/callout with fixed-accuracy voting, chance-level comparison and correlated-error limits. Source-reviewed against each route's actual retained article and controls. No new wrapper, control, CSS, dependency, request, persistent state or runtime logic. Text length can change authored wrapping/vertical flow; geometry and payload acceptance are not measured.
- Static checks: `npm --prefix interactive-explanation run check` exit0; `npm --prefix interactive-explanation run unit` exit0, 373/373; `node interactive-explanation/tools/check-public-surface.mjs interactive-explanation` exit0; `git diff --check` exit0. No separate lint/typecheck is configured.
- Read-only Node assertions compare both route documents against HEAD: every complete script element byte-identical and in the same order, including inline scripts; every HTML tag/attribute identical, preserving mount IDs, metadata, navigation and control declarations. Matching parity JSON parses and equals HEAD after removing exactly the one added W2 note per file. No runtime state preservation is inferred as a measured browser result.
- Self-review: authored claims are bounded, existing links/section order/engines/provenance preserved; no independent or human acceptance claim. Browser ownership was not established, so no browser was launched and no concurrent browser work introduced. Focused strict, visual/theme/viewport/input, geometry, paired timing, full smoke and other master qualification remain pending. No baseline written or accepted.
- Exact authored remainder (29): `linear-regression`, `logistic-regression`, `precision-recall`, `roc-auc`, `train-test-validation`, `double-descent`, `double-descent2`, `conditional-probability`, `principal-component-analysis`, `exponentiation`, `pi`, `sine-and-cosine`, `eigenvectors-and-eigenvalues`, `image-kernels`, `ordinary-least-squares-regression`, `alpha-compositing`, `color-spaces`, `sound`, `cameras-and-lenses`, `lights-and-shadows`, `tesseract`, `gears`, `gps`, `earth-and-sun`, `curves-and-surfaces`, `music-interactive-hub`, `primary-interactive-hub`, `hysteresis-slack`, `blockchain-101-combined-flow`.
- Acceptance remainder: all31. Next: establish exclusive browser ownership and run `node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route decision-tree --route random-forest --experience`; review resulting authored geometry separately without changing baselines. Resolve or explicitly coordinate the first-slice gate before expansion; then continue same-family source units of at most five. No commits, installs, shared/manifest/minified/baseline edits or package03 changes.

## Remaining29 implementation checkpoint — 2026-09-13

- User explicitly supersedes the earlier expansion stop for source implementation: finish all remaining29 in isolated same-family units, no browsers; parent owns deferred browser qualification. W0 timing/review and W2 acceptance remain unresolved, not waived or passed.
- Same worktree and HEAD `6111af8d7a53a1e716f3b7b784072c3c55c3d45a`. Existing unrelated dirty package03/05/06, shared/Atlas, route and evidence work remains untouched. This session owns only the following29 route `index.html` files, matching `docs/<slug>/parity.json` and this checkpoint. Earlier decision-tree/random-forest edits retained without modification. Preimages for the remaining29 route/parity pairs are HEAD blobs; exact script/tag/style and retained-parity comparisons pass against them.
- Slice 2, mlu-pilot (5): `linear-regression`, `logistic-regression`, `precision-recall`, `roc-auc`, `train-test-validation`. Fit/residual loss, threshold versus probability, confusion counts, ROC denominators, and validation/test roles respectively. Native TTV TOC and all compiled mounts retained. Browser qualification pending with parent for every member.
- Slice 3, mlu-pilot (2): `double-descent`, `double-descent2`. Between-sample interpolation comparisons and minimum-norm/knot assumptions; no universal monotonic complexity claim. Browser qualification pending with parent for both members.
- Slice 4, ev-essay (5): `conditional-probability`, `principal-component-analysis`, `exponentiation`, `pi`, `sine-and-cosine`. Conditional denominators, projected variance, equal-step growth, circumference scaling and angle/ratio/coordinate comparisons. Source review corrected the sine task to dragging the angle, not an unsupported independent triangle resize. Browser qualification pending with parent for every member.
- Slice 5, ev-essay (3): `eigenvectors-and-eigenvalues`, `image-kernels`, `ordinary-least-squares-regression`. Nonzero collinearity/complex rotation, nine-weight arithmetic/black padding and one-sample residual influence. Browser qualification pending with parent for every member.
- Slice 6, engineering-longform (5): `alpha-compositing`, `color-spaces`, `sound`, `cameras-and-lenses`, `lights-and-shadows`. Coverage versus opacity, RGB encoding, pressure propagation, focus/aperture and source-size shadow comparisons; bounded physical/display/audio claims. Browser qualification pending with parent for every member, including sound audio/input coverage.
- Slice 7, engineering-longform (5): `tesseract`, `gears`, `gps`, `earth-and-sun`, `curves-and-surfaces`. Slice versus viewpoint, rim travel versus angle, range constraints/clock bias, date versus UTC time, and control-point influence. Browser qualification pending with parent for every member.
- Slice 8, local-hub (2): `music-interactive-hub`, `primary-interactive-hub`. Existing hero/path-note copy becomes task-led discovery without adding essay scaffolding. Music five-stop path, links, continuation and existing 30-day Progress preserved; Primary remains unnumbered without Progress. Browser qualification pending with parent for both members.
- Slice 9, systems-essay (1): `hysteresis-slack`. Opposite-direction approaches compare outputs at the same input; mechanical slack and mood analogy explicitly bounded. Browser qualification pending with parent.
- Slice 10, anders-lab (1): `blockchain-101-combined-flow`. Existing hero/callout and Suggested order distinguish hash linkage, signature verification and limited disclosure; removes the immutable-record implication. Linked engines and three-step Progress remain autonomous and unchanged. Browser/linked-child qualification pending with parent.
- Changes are text nodes in existing heroes/callouts only, plus one additive route-specific W2 parity note per pair. Each note distinguishes new interpretation/adapted framing from retained scenes and marks prior evidence historical. Exact preservation: all HTML tags/attributes, complete script elements in order (including inline MathJax), style blocks, mounts, metadata, native handlers and link targets. No new persistent state, control, wrapper, comment, payload class, dependency or request. Copy length/UTF-8 bytes and line wrapping may change; no geometry/timing claim.
- Static gates on the current dirty integration tree: `npm --prefix interactive-explanation run check` exit0; `npm --prefix interactive-explanation run unit` exit0, 373/373; `node interactive-explanation/tools/check-public-surface.mjs interactive-explanation` exit0; `git diff --check` exit0. No separate lint/typecheck configured. No npm test/sync, browser, install, commit, baseline recording or engine/shared/manifest edit performed.
- Exact31 read-only Node invariant run passed every member: extract canonical package membership, assert31 unique, require changed HTML, compare complete scripts/order, all tags/attributes and style blocks to HEAD; parse parity and require exactly one W2 note in the first module, then deep-compare all retained modules to HEAD. Manifest shell-only ownership matches and family totals are mlu9/ev8/engineering10/hubs2/systems1/anders1. These are source invariants, not measured runtime/state/theme acceptance.
- Source self-review only: tasks correspond to retained route content and controls; no independent or human acceptance claimed. No non-trivial application logic added, so no new test file or framework required.
- Exact coverage: remaining29 implemented this session, prior2 retained, total31/31 authored route/parity pairs (62 application files) plus this existing checkpoint. Exact implementation remainder: none. Qualification remainder: all31, including focused strict/full smoke, geometry/visual, paired timing, theme/input/zoom/AT/device/child and editorial review as applicable. Parent next action is serialized qualification with explicit slice membership and preserved baselines; source completion is not W2 acceptance.
