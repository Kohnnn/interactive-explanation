# 03 Intent pilots

Status: ready-for-agent
Spec: [Master specification](../spec.md)
Blocked by: [02 Shared system and Atlas](02-shared-system-and-atlas.md)
Execution: not started
Wave: W1 pilots — exactly 3 Routes

## Exact membership by canonical family

- mlu-pilot: `bias-variance`
- anders-lab: `blockchain`
- teoria-practice: `teoria-scale-construction`

These are the three pilot rows in the [canonical planning snapshot](../research/35-route-rollout.json), appearing nowhere else in implementation packages 04–06. Atlas belongs only to 02.

## Slice sequence and scope

Begin only after shared-system acceptance. Implement one pilot per slice: bias-variance, then blockchain, then teoria-scale-construction; gate each before the next, not all three as one session.
Preserve shell-only ownership for bias-variance and declared runtime hooks for blockchain and teoria-scale-construction. Inspect the actual authored seams before editing; compiled engines remain untouched.
Adapt question/action/consequence/reason/optional-transfer ingredients differently by intent, not a shared wizard or promoted prototype runtime.
Essay: orient, expose assumptions, compare observations, interpret. Bias² + variance + noise requires squared error at a fixed input with stated expectation/noise assumptions; no universal monotonic complexity rule.
The sample fixed-target arrays demonstrate empirical bias² + variance with no target noise; they do not replace production charts or prove generalization.
Lab: direct manipulation, visible dependency state, explanatory invalid-operation feedback, safe recovery. Preserve real hash behavior; the prototype receipt flags are explicitly non-cryptographic and never an engine replacement or security/consensus proof.
Practice: short attempt, recoverable specific feedback, optional hint and independent retry. Major scales use W W H W W W H; D major is D E F# G A B C# D with successive letters.
Preserve existing scoring, audio/notation, reveal, reset, and mount state. The sample exercise adds no persistent Progress or fake audio.

## Observable acceptance

- Each pilot meets all applicable master states in both themes, three viewports, keyboard/pointer/touch, saved/system theme, reduced motion and zoom; labels and feedback are not color-only.
- Theme/reflow preserves results, score/work state, engine dimensions, pointer mapping, script dependencies, local data, URLs/deep links and continuation target.
- Contrast/targets meet master AA requirements and 44px design target. No clipping, new unexpected runtime/network failures, or unexplained geometry.
- Paired three-fresh-context measurements use the same provisioned environment; declare resources and retain inherited relative DCL/load and transfer ceilings, median/range and unsupported limitations.
- Record retained/adapted/new prose and actual behavior in Route provenance/parity; preserve attribution/license obligations even when a legacy audit conflicts.
- Review teaching accuracy and intent as agent editorial judgment, not human approval or proven learning improvement. Material out-of-scope changes require a human decision.

## Commands and checkpoints

From parent `interactive-note/` root, replace `<slug>` with the current exact pilot:
`npm --prefix interactive-explanation run check`
`npm --prefix interactive-explanation run unit`
`node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation --route <slug> --experience`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation`
Focused gates run per pilot; full regression runs after shared changes and at W1 closure. Add a small existing-seam runnable check for genuinely new non-trivial authored logic.
Append each pilot checkpoint here: completed/remaining slugs, owned preimages/hunks, source/environment, results/evidence, modality gaps, reflow successor decision, next action.
Resume from the last passing pilot; rollback only its own edits, retaining dirty work, data and baseline history. Stop for incompatible browser, drift, hard regression, uncertain license, or required engine edits.
All three must pass before package 04. Blocked device/child/accessibility modalities remain explicit final-qualification blockers; no automatic install, commit, deployment, or release.

## Flattened repository port — 2026-09-13

Imported append-only from the frozen parent integration at `6111af8d7a53a1e716f3b7b784072c3c55c3d45a` plus authored edits onto live `bbf680a92b25adbf5ca302268d7e7a94dec06cc8`. Earlier header status is historical: all three authored pilots are implemented, none accepted. Parent `interactive-explanation/` paths map to this repository root; parent Git commands and reported checks remain historical parent-context evidence. Use root npm scripts and `node tools/smoke-bundle.mjs .` here. No browser or acceptance result is implied by this port.

## Execution checkpoint — bounded authored integration, 2026-09-13

### Authorization, ownership and preimages

- User explicitly requested full authored redesign now despite historical W0 timing and unavailable review, with browser work deferred to the parent. This supersedes the implementation timing/one-browser-gated-pilot-at-a-time stop for this bounded session, not the acceptance requirements. Shared Editorial CSS was already parent-owned work; it was not edited here.
- Worktree: `/tmp/opencode/complete-route-integration`; HEAD `6111af8d7a53a1e716f3b7b784072c3c55c3d45a`. Linux, Node `v24.20.0`; no browser launched or installed. Initial status contained 54 modified files plus an untracked `interactive-explanation/node_modules` entry. None of these seven owned paths had a diff before editing; their exact preimages remain the HEAD blobs.
- Owned paths only: `interactive-explanation/{bias-variance,blockchain,teoria-scale-construction}/index.html`, their three `interactive-explanation/docs/<slug>/parity.json` records, and this existing checkpoint. No shared, manifest, baseline, dependency, vendor, sibling page or runtime changes; no commits. Rollback only these session hunks after checking concurrent drift, never reset the dirty tree.
- Completed authored slices, in order: `bias-variance`, `blockchain`, `teoria-scale-construction`. Remaining authored slugs in this bounded request: none. Accepted slugs: none. Remaining package qualification: all three, plus inherited package 02/shared acceptance. Package 04 is not authorized by this checkpoint.

### Exact edits and applicability

- `bias-variance`: question-first hero, removed chips, conditional-mean/fixed-input assumptions before charts, corrected squared-bias/variance expectations, scoped illustrative complexity claims, validation guidance, LOESS tendencies, classification-loss distinction and non-monotonic double-descent interpretation. Intro heading demoted with ID retained. All chart mounts, scroll step attributes, KaTeX targets and scripts remain. No new diagram or model computation. Applicable: scroll/readiness, generated chapters, randomization/sliders, fallback, intrinsic geometry and KNN input access; audio/scoring/reveal/Progress not applicable because none added.
- `blockchain`: replaced hero and two redundant panels with one edit/compare/recompute brief; removed duplicate continuation buttons, not native navigation. Corrected immediate Prev propagation and 0000 local validity; explained per-block mining, chance-valid hashes, 500000 cap, left-to-right recovery and reload data loss. Explicit SHA256 toy-ledger boundary, no immutability/consensus/security/finality proof. Five forms and all values, handlers, scripts and row geometry markup retained. Applicable: edit/paste, mining/busy/exhaustion, valid/invalid text, reload, horizontal access, native/sibling navigation; audio/score/reveal/Progress not applicable.
- `teoria-scale-construction`: shortened hero, removed repeated onboarding/strip/chips, clarified prompt type and spelling versus sound, demoted practice header to h2. Added native closed-by-default details after the unchanged mounts: separate written D-major attempt, optional exact `D E F# G A B C# D` / `W W H W W W H` explanation and F/C-natural versus Gb/Db correction. No extra answer engine: disclosure state only, no score/audio/Progress writes. Applicable: disclosure keyboard/pointer/touch, runtime load/options/attempt/correct/incorrect/reveal/next/score, audio play/stop/unavailable and notation continuity. Automated scoring/playback/persistence for the new reference is not applicable because it is only a worked disclosure.
- Each parity file prepends `package03-editorial-pilot-partial` recording retained/adapted/new material, sources, changed behavior, applicability and open findings. Older modules remain historical evidence, explicitly not acceptance of this revision.
- Expected delta: shorter authored content and fewer panels change page height, anchor locations and scroll pacing; practice adds one native disclosure. No external script/style/media request added or removed by these HTML edits. Exact HTML byte changes: bias-variance 25204 to 24661; blockchain 16230 to 15155; teoria-scale-construction 7125 to 6172. These file sizes are not measured network transfer or performance gains.
- Post-edit Git content IDs: bias HTML `b54c06447343ce2f0894fa2aef9d59d688d00d38`; blockchain HTML `c240ef9163acdd92ad158a56ab92266abc23a03d`; scale HTML `74b26ee2bfedf818be167f0aedb4d8adda9448aa`. Parent-owned shared CSS observed at `7f883a441c3a63975c08b8cd166af9dd568b922e`; its review/acceptance is not inherited here.

### Static results and review

- From `interactive-explanation/`: `rtk npm run check` PASS; `rtk npm run unit` PASS, 373/373 with zero failures/skips; `rtk npm run audit` PASS. These are syntax, Node unit and policy checks, not browser exercise acceptance. No separate lint/typecheck is configured. `npm test` and synchronization were deliberately not run.
- Read-only Node assertion against each HEAD HTML PASS: every complete script tag/body/order exactly equal, all inline event handlers equal, every original ID retained, every inline script parses via `vm.Script`, all three parity JSON files parse. `git diff --check` PASS. Console output in this session is the evidence; no separate evidence file or baseline was written.
- Reproducible script-preservation check from the worktree root:

```bash
node --input-type=module -e 'import fs from "node:fs"; import assert from "node:assert/strict"; import {execFileSync} from "node:child_process"; import vm from "node:vm"; for (const slug of ["bias-variance","blockchain","teoria-scale-construction"]) { const p=`interactive-explanation/${slug}/index.html`; const before=execFileSync("git",["show",`6111af8d7a53a1e716f3b7b784072c3c55c3d45a:${p}`],{encoding:"utf8"}); const after=fs.readFileSync(p,"utf8"); const matches=(s,re)=>[...s.matchAll(re)].map(m=>m[0]); assert.deepEqual(matches(after,/<script\b[^>]*>[\s\S]*?<\/script>/g),matches(before,/<script\b[^>]*>[\s\S]*?<\/script>/g)); assert.deepEqual(matches(after,/\bon\w+="[^"]*"/g),matches(before,/\bon\w+="[^"]*"/g)); for(const id of matches(before,/\bid="[^"]*"/g)) assert.ok(after.includes(id),`${slug}: ${id}`); for(const m of after.matchAll(/<script\b([^>]*)>([\s\S]*?)<\/script>/g)) if(!/\bsrc=/.test(m[1])) new vm.Script(m[2]); JSON.parse(fs.readFileSync(`interactive-explanation/docs/${slug}/parity.json`,"utf8")); console.log(`${slug}: preservation checks passed`); }'
```

- Standards/source self-review: reused existing classes/native details, retained runtime boundaries and references, no added comments/dependencies/logic framework. Spec self-review: authored pilot claims and distinct intent match lines 99–105, but all browser acceptance obligations remain partial. No independent sub-agent review was available; no human review or learner outcomes claimed.
- Incidental read-only inspection failures: `python` unavailable, then an extra closing parenthesis in a Node display command; corrected with a working Node command. Neither touched files or represents an application failure.

### Honest open findings and next safe action

- Parent must qualify all three at stored light/dark × 1400×1000, 390×844, 320×844; system fallback, denied storage, persisted/live theme, keyboard/pointer/touch, focus/contrast/target sizing, zoom and reduced motion; authored and intrinsic state retention, network/console, continuation readiness, paired geometry and three-fresh-context performance. No browser samples were taken here. Existing baselines stay unchanged; any intentional-reflow successor requires separate approval/evidence.
- Bias: changed step prose may change scroll-trigger timing. Intrinsic charts/KaTeX, retained illustrative heuristics and hover-only KNN accessibility remain unqualified, not fixed by explanatory copy.
- Blockchain: retained keyup handlers may miss pointer/paste input paths; capped mining can leave validity labels stale on exhaustion because state updates only on success. Readers are directed to the actual prefix, but these runtime limitations remain open for parent triage. Retained duplicate `id="state"` markup is not corrected in this authored-only slice.
- Practice: no browser evidence for generated correction/reveal accessibility, score continuity, audio fallback, notation dimensions or the new summary target/focus treatment. The worked reference does not substitute for engine-specific feedback validation.
- Next safe action: parent checks source drift against the recorded HTML identities, runs focused strict pilot browser checks and the shared/full W1 gate, records raw before/after evidence and resolves findings. Continue package 03 as partial implementation, not acceptance or release.
