# 02 Shared system and Atlas

Status: ready-for-agent
Spec: [Master specification](../spec.md)
Blocked by: [01 Restore release evidence](01-restore-release-evidence.md)
Execution: not started
Wave: W1 shared foundation — zero Route implementation assignments

## Scope and next-session boundary

Atlas is implemented only here; all 83 Route implementations belong to packages 03–06. Reuse existing authored shared seams rather than adding a component runtime.
After restored evidence, inventory current shared selectors and ownership. First slice: settled dual-theme token mapping and one Atlas composition; serialize shared edits and gate before propagation.
Apply Editorial brief A: dominant question, fine rules, functional groups, system sans/serif/mono roles, orange action and blue explanatory data with textual meaning.
Use the master token/geometry contract; do not copy prototype descendant selectors, switcher, memory-only theme state, or illustrative frame dimensions.
Preserve existing pre-paint saved preference, system fallback, storage-denial behavior, and theme persistence across navigation.
Keep Atlas inventory, discovery filters, clear/no-results behavior, local links, and existing card intent semantics sourced from canonical metadata.
Keep one named Suggested Next Route per Route, existing navigation ownership, and Guided Path-only Progress semantics while adapting authored shared chrome.

## Ownership and observable acceptance

- Shared styles cannot propagate into intrinsic/opaque descendants or independent children. No forced wrapper, global reset, new font, analytics, framework, or runtime dependency.
- Atlas search/filter/reset and local navigation remain keyboard/touch operable; readable empty/error states recover without lost focus or user data.
- Both themes meet the master component/state matrix at 1400×1000, 390×844, and 320×844, with system fallback, saved navigation, reduced motion, focus and zoom review.
- Text contrast meets 4.5:1 or applicable large-text 3:1; controls/non-text meet applicable 3:1. Targets meet 24px AA minimum or documented exception, with 44px design target.
- No masked overflow, obscured focus, new runtime/network failure, or unexplained geometry. Runtime dimensions, pointer mapping, mounting, and state remain unchanged.
- Declare request/transfer deltas and paired evidence. Retain DCL/load increase ceiling max(20%,250ms) and transfer max(20%,250KiB), using corresponding before values, not fixed snapshot timings.
- Required attribution/license material remains even if policy requires a narrow documented correction. Never strip source obligations for a visual cleanup.

## Commands and exit

Run from parent `interactive-note/` root:
`npm --prefix interactive-explanation run check`
`npm --prefix interactive-explanation run unit`
`node interactive-explanation/tools/check-public-surface.mjs interactive-explanation`
`node interactive-explanation/tools/smoke-bundle.mjs interactive-explanation`
Use existing browser probes for the Atlas matrix and `--route <migrated-slug> --experience` with the smoke command for affected migrated surfaces.
Every shared change requires full 83-Route regression, not only Atlas or a pilot. Package 03 stays blocked until this package passes; unresolved hard failures cannot be waived by token contrast.

## Checkpoint and rollback

At each session end append completed shared behaviors, owned hunks/preimages, source identity, commands/evidence, matrix gaps, baseline decision, and exact next slice here.
Resume by verifying unchanged inputs and previous gate; preserve all unrelated dirty work and local data. Reverse only owned hunks if a shared regression appears; stop for overlapping edits.
Approve intentional authored reflow through recorded agent review and a separate successor baseline retaining history; request human decision for material out-of-scope departures.
Blocked child/device/assistive-technology modalities remain explicit qualification obligations. No blanket accessibility, learning or field-performance claim; no automatic install, commit, deployment, or release.
