# Migrate systems Routes

Type: task
Status: resolved
Blocked by: 18

## Question

Complete and verify `memory-allocation`, `load-balancing`, `hysteresis-slack`, and `rigid-body-collisions`. Preserve allocator and load-balancer state, Pixi/Plotly and compiled output, delayed mounts, Nuxt lifecycle, canvas geometry, and generated chapter behavior sourced from the manifest. Update parity records and leave one atomic family commit after focused and full gates.

## Answer

Migrated all four systems Routes with exact manifest-owned chapter contracts and root-scoped family theming. Mapped the canonical pre-paint saved theme into the existing SamWho runtime hooks, themed only authored Hysteresis prose, left Nuxt/WebGL output intrinsic, and raised only generated shell navigation above the Rigid Body runtime. Preserved allocator, Pixi, Plotly, compiled D3/React, Nuxt, WebGL, control, and intrinsic geometry ownership; updated all four parity records and exact chapter tests.

Verified with `npm test` (363 passing tests), metadata synchronization, public-surface and syntax checks, `git diff --check`, the combined four-Route strict experience gate, final green Standards/Spec reviews, and the full 83-Route smoke suite under reversible host scheduler isolation with every surviving process setting restored.
