# Migrate MLU Explain Routes

Type: task
Status: resolved
Blocked by: 15

## Question

Complete and verify `decision-tree`, `random-forest`, `linear-regression`, `logistic-regression`, `precision-recall`, `roc-auc`, `bias-variance`, `train-test-validation`, `double-descent`, and `double-descent2`. Use manifest chapters for generated Routes, preserve the native TOC on `train-test-validation`, theme only authored HTML and verified controls, and leave compiled scenes intrinsic. Update parity records and leave one atomic family commit after focused and full gates.

## Answer

Migrated all ten MLU Routes with one exact-family token adapter for shared chrome, authored prose, and verified controls. Nine Routes use manifest-owned generated chapters; `train-test-validation` retains its seven-link native TOC, preserves compiled `data-page` scene ownership, resolves Summary to `#summary`, and keeps the same controls visible at mobile widths without moving intrinsic geometry.

Compiled scenes, SVG, canvas, images, KaTeX, dimensions, and transforms remain intrinsic. Route-local hidden iframe title adapters preserve compiled resize listeners without bundle edits. Narrow containment fixes only Linear Regression's equation overflow and Logistic Regression's compiled app overflow. Bias-Variance uses one inline, zero-request compatibility adapter to initialize missing Scatter and ErrorBar prerequisites after cold continuation jumps while leaving its compiled bundle untouched.

Verified with the focused Train/Test/Validation strict gate, the ten-Route MLU strict `--experience` gate, `npm test` with 362 passing tests, synchronized 83-Route metadata, public-surface and syntax checks, `git diff --check`, and the full 83-Route smoke suite with persisted geometry and comparative performance enforcement.
