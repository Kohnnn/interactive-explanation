# Migrate Anders Brownworth labs

Type: task
Status: resolved
Blocked by: 15

## Question

Complete and verify `blockchain`, `public-private-keys`, and `zero-knowledge-proof-demo`. Preserve direct-child Bootstrap structures, hash/crypto state, native route navigation, map plugin ownership, and all control dimensions; theme only authored lab HTML and declared state classes. Update parity records and leave one atomic family commit after focused and full gates.

## Answer

Migrated all three Anders labs through the declared `.story-lab-main` runtime hook. The exact family adapter themes authored Bootstrap panels, fields, controls, and semantic success/error classes while stopping at the jVectorMap root. Existing Bootstrap collapses remain the native navigation state owners, start expanded for complete narrow-viewport access, and retain keyboard collapse/re-expand behavior. Hash propagation, nonce mining, key generation, signatures, tamper failure, transactions, palette state, region selection, map reset, plugin geometry, and control dimensions remain Route-owned.

Verified with `npm test` (363 passing tests), metadata synchronization, policy and syntax checks, `git diff --check`, the combined three-Route strict experience gate, the full 83-Route smoke suite under reversible scheduler isolation, and final green Standards/Spec reviews.
