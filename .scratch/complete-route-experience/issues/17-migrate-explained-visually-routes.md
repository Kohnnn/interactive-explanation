# Migrate Explained Visually Routes

Type: task
Status: resolved
Blocked by: 16

## Question

Complete and verify `conditional-probability`, `markov-chains`, `principal-component-analysis`, `exponentiation`, `pi`, `sine-and-cosine`, `eigenvectors-and-eigenvalues`, `image-kernels`, and `ordinary-least-squares-regression`. Scope adapters to authored controls and declared roots; preserve SVG, canvas, WebGL, iframe, media, and MathJax output and geometry unless an existing hook is declared. Update parity records and leave one atomic family commit after focused and full gates.

## Answer

Migrated all nine Explained Visually Routes with one exact-family `ev-essay` adapter for shared chrome, authored prose, labels, links, and native controls. Manifest-owned generated chapters use existing authored IDs where available and deterministic IDs only for ID-less top-level chapter roots.

SVG, canvas, WebGL, iframe, media, MathJax, custom runtime elements, dimensions, and transforms remain intrinsic. Narrow containment stays on declared authored roots, while route-specific title spacing prevents Exponentiation heading overflow without changing runtime geometry.

Verified with the nine-Route Setosa strict `--experience` gate, synchronized 83-Route metadata, `npm test`, public-surface and syntax checks, `git diff --check`, independent standards/spec reviews, and the full 83-Route smoke suite with persisted geometry and comparative performance enforcement.
